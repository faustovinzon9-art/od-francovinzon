# Pendientes activos

**Nuevo pedido (2026-08-13, décima vuelta) — Fichas (mayúsculas/Title Case, tamaños), encabezado de /turnos, subtítulo de "Guardar acceso", botón Confirmar solo cerca de la fecha — completo, corrido de punta a punta sin pausas a pedido explícito del usuario**.
1. ✅ **Fichas: ya no fuerzan mayúscula al tipear** (nombre, apellido, localidad, domicilio, obra social, plan, tratamiento) — se escribe libre, tanto en el panel rápido de "nuevo paciente" como en la ficha completa y en la revisión de fusión de duplicados. **Se normaliza a Title Case recién al guardar**, server-side (`aTituloCase()` nuevo en `lib/pacientesSheet.js`, usado en `crearPaciente`/`escribirCampoEnSheet` de `api/gestion/pacientes.js`) — mismo criterio ya establecido de que el backend es la fuente de verdad del formato, el cliente ya no transforma nada. `aMayusculas()` se deja intacto y se sigue usando tal cual para movimientos/prestaciones (tratamiento, forma de pago, código de obra social) — **fuera del alcance de este pedido**, siguen en mayúscula total a propósito.
2. ✅ **Migración de una sola vez de las fichas ya cargadas** a Title Case — corrida contra producción real con `CRON_SECRET` (mismo mecanismo que el cron de salud), en lotes paginados para no arriesgar el timeout de una función serverless. Endpoint temporal (`modo=migrar-titulo-una-vez` en `api/gestion/pacientes.js`) **ya borrado del código** después de correrla — ver el resumen de la corrida más abajo en este mismo archivo o en `changelog.md`.
3. ✅ **Tarjeta "Datos del paciente" más compacta**: de 340px a 296px de ancho en la columna izquierda (desktop, `≥1000px`) — achicado moderado, no drástico.
4. ✅ **Letra más grande donde Franco la necesita** (usa lupa): nombre+apellido del título de la ficha (22px→27px), el bloque de Saldo/estado financiero (label 10.5px→11.5px, valor 17px→23px, pill "AL DÍA"/"DEBE" 12px→14.5px), y la tabla de movimientos (12.5px→15px, encabezados 10px→12px). El resto de la ficha queda igual, a propósito.
5. ✅ **Encabezado de `/turnos` limpiado**: sacado el texto de "todos los horarios están fijos en hora Argentina" y la dirección "Ameghino 410" (CSS de `.tz-note`/`.brand .subtitle` eliminado por quedar sin uso). Redacción del texto informativo ajustada a un tono más cálido/directo ("¿Urgencia o consulta rápida? Usá los botones de abajo"). La tarjeta de horarios encontrados (`#slots-card`) ahora tiene borde dorado, sombra más marcada y título más grande en navy/serif — antes era un título gris chico sobre una tarjeta blanca igual a cualquier otra, pasaba desapercibida.
6. ✅ **Subtítulo debajo de "Guardar acceso a mi turno"**: "Guardalo para poder cancelar o cambiar tu turno cuando quieras." — chico, gris, en su propio grupo con el botón para no romper el espaciado del resto de las acciones.
7. ✅ **Botón "Confirmo el turno" solo aparece dentro de las 72hs previas al turno** (`turno/index.html`, `dentroDeVentanaConfirmar()`), calculado con el mismo patrón de timezone fijo Argentina (UTC-3, sin DST, `limitesUTC()` ya existente) — nunca con la hora local del navegador. Mismo link siempre (no hay una versión distinta para "guardar" vs. recordatorio de WhatsApp): la aparición del botón depende únicamente de qué tan cerca está la fecha en el momento en que se abre el link, verificado en los dos puntos donde se muestra `#post-actions-card` (al aterrizar vía el link y después de reprogramar).

**Cómo se probó (sin pausar a pedir confirmación, corrido de punta a punta)**:
- `aTituloCase()` y `dentroDeVentanaConfirmar()` verificados con un test aislado de las funciones puras (varios casos, incluido el cruce de día en la ventana de 72hs) — todos los resultados correctos.
- Encabezado de `/turnos` y la tarjeta de horarios (borde dorado) confirmados visualmente con captura de pantalla real.
- Subtítulo de "Guardar acceso a mi turno" confirmado visualmente con captura de pantalla real.
- Los tamaños/ancho de la ficha de paciente **se verificaron por revisión directa del código** (valores CSS concretos, cambios simples de un solo número) — la captura de pantalla de esa página específica tuvo un problema puntual de renderizado en la herramienta de este entorno (el contenido se confirmó correcto por texto, no por píxeles). Mismo tipo de cambio (tamaños de fuente) ya se había confirmado visualmente sin problemas en `/gestion` en una vuelta anterior.
- La migración se corrió primero en `dryRun=1` contra producción real, se revisó el resultado, y recién después se corrió de verdad.

**Nuevo pedido (2026-08-12, novena vuelta) — Link corto de gestión de turno (/t/CODIGO) + tono del link en el mensaje de WhatsApp**: rama `feature/link-corto-turno`. **Sin confirmar en vivo contra Calendar real** (este entorno no tiene credenciales de Google ni acceso a la API/dashboard de Vercel) — ver el detalle exacto de qué se verificó y qué falta más abajo.

1. ✅ **Link corto `/t/CODIGO`**: código random de 6 caracteres (alfabeto sin 0/O/1/I/L para que sea legible), guardado como `Código corto: XXXXXX` en la description del evento — mismo patrón `Etiqueta: valor` que Teléfono/DNI/Motivo. Se genera al crear el turno (`crearTurno()` en `lib/googleCalendar.js`, y también en `api/gestion/crear-turno.js` que es un endpoint de creación aparte que usa `/gestion`). Para turnos viejos sin código, se genera "al vuelo" la primera vez que hace falta (`asegurarCodigoCorto()`, idempotente — si ya existe no escribe nada) — no hay ninguna migración masiva. Sin chequeo de colisión: 6 caracteres de un alfabeto de 32 dan ~1.073 millones de combinaciones, de sobra para este volumen.
2. ✅ **Endpoint público `/t/:codigo`**: rewrite en `vercel.json` (`/t/:codigo` → `/api/reservar?codigo=:codigo`) — **no se creó ningún archivo nuevo en `api/`**, sigue en 12 funciones (límite del plan Hobby, ver `architecture.md`). Busca el código en los dos calendarios con el parámetro `q` de la Calendar API (sin límite de fecha, mismo criterio que `telefonoDesdeTurnos`), confirma el match EXACTO contra la description (`q` es búsqueda difusa, podría traer falsos positivos) y recién ahí redirige (302) al link largo real. Si no lo encuentra (o el turno está cancelado), muestra una página HTML simple con "Este link ya no es válido" + botón de WhatsApp — generada inline en `api/reservar.js`, sin depender de ningún archivo estático nuevo.
3. ✅ **Link corto usado en todos lados**: botón "Guardar acceso a mi turno" en `/turnos` (con fallback al link largo si por algún motivo no vino el código en la respuesta), mensaje de WhatsApp de `/gestion`, y QR del ticket térmico (menos denso, más fácil de leer).
4. ✅ **Reescrita SOLO la línea del link en el mensaje de WhatsApp** (`mensajeRecordatorio()` en `gestion/index.html`) — el saludo, el nombre, la fecha/hora y el cierre quedan exactamente igual (la voz de Ayelen, no se tocó). La línea acartonada ("Es importante que entres a este link para confirmar...") pasó a "Tocá este link para confirmarlo:", link en su propia línea. **El 👇 que sugería el pedido original se dejó afuera a propósito** — mismo bug ya comprobado varias veces (ver `decisions.md`): `wa.me`/`api.whatsapp.com` corrompe cualquier emoji en el parámetro `text` antes de llegar al chat. Se avisa explícitamente, no es un olvido.
5. ✅ **Meta tags Open Graph agregadas a `turno/index.html`** (`og:title`, `og:description`, `og:image`, `og:type`) — la página no tenía ninguna, así que la vista previa de WhatsApp del link corto (que redirige ahí) mostraría una tarjeta gris genérica con el dominio de Vercel pelado. No se pudo confirmar visualmente cómo queda la preview real en WhatsApp desde acá (necesita mandar el link desde un WhatsApp real).
6. El botón de WhatsApp en la fila de `/gestion` pasó de `<a href>` armado de antemano a `<button data-action="enviar-whatsapp">` que arma el mensaje (ahora async, por el link corto) recién al tocar — mismo patrón `data-action` ya usado para el resto de los botones de la fila.

**Qué se pudo verificar y qué no (este entorno no tiene credenciales de Google, ni acceso a la API de Vercel/dashboard, así que nada de esto se pudo probar contra Calendar real)**:
- ✅ Con un test aislado (sin depender de la página completa) se confirmó: `generarCodigoCorto()` genera siempre 6 caracteres del alfabeto correcto, sin colisiones en una muestra de 20; `extraerCodigoCorto()` extrae bien el código en 4 casos (con tilde, sin tilde, minúscula, sin código presente).
- ✅ Balance de sintaxis (paréntesis/llaves) y JSON de `vercel.json` verificados en todos los archivos tocados.
- ✅ Conteo de funciones en `api/` confirmado en 12 (sin archivos nuevos) — pero **esto no reemplaza correr el deploy de preview real**, que es la única forma de confirmar que Vercel no tira el error de límite en la práctica.
- ❌ **No probado**: crear un turno real y confirmar que el código corto se genera y se guarda en la description real. **No probado**: que `/t/CODIGO` efectivamente redirija al link largo contra Calendar real. **No probado**: tocar el link corto desde WhatsApp en un celular real. **No probado**: cómo se ve la vista previa de WhatsApp del link corto. **No probado**: qué pasa con un código inválido/viejo contra el endpoint real (la página de error se armó y se revisó a mano, pero no se disparó nunca contra el flujo real).
- Antes de mergear, hace falta correr estos pasos vos mismo (o pedírmelo cuando haya forma de probarlo en vivo): crear un turno de prueba, revisar en el evento de Calendar que la línea `Código corto: XXXXXX` se guardó, entrar a `https://od-francovinzon.vercel.app/t/XXXXXX` (con el código real) y confirmar que redirige a `/turno?eventId=...`, mandarte el mensaje de WhatsApp real por acá para confirmar cómo quedó, y entrar a un código inventado (ej. `/t/ZZZZZZ`) para confirmar que se ve la página de error y no algo roto.

**Nuevo pedido (2026-08-12, octava vuelta) — Tarjeta de confirmación de /turnos (cartel de horarios, compartir acceso, calendario unificado) + 2 ajustes en /gestion, completo**: rama `feature/turno-confirmacion-mejoras`, probado offline (ver abajo el detalle de qué se pudo y qué no se pudo confirmar en vivo), pendiente el visto bueno en preview antes de mergear.
1. ✅ **Cartel de "respetá los horarios" movido adentro de la tarjeta de confirmación**: se eliminó `#respeto-horarios-card` (tarjeta aparte, quedaba perdida al final) y se agregó `<p class="success-warning">` justo debajo del título, tanto en `#success-card` ("¡Turno confirmado!") como en `#success-card-nuevo` ("¡Bienvenido/a!", para que el paciente nuevo no pierda el aviso). Texto acortado a una frase directa. Color rojo terracota (`#A6483A`) intencionalmente distinto del rojo de error/alerta (`#C14437`) ya usado en el sitio, para que no lea como "algo salió mal".
2. ✅ **Botón de WhatsApp reemplazado por "Guardar acceso a mi turno"** (`turnos/index.html`): usa `navigator.share()` nativo con `title`/`text`/`url` (el link permanente `/turno?eventId=...`, mismo destino que el QR y el recordatorio de WhatsApp). El link **nunca se muestra como texto en pantalla** — solo viaja dentro del share nativo o se copia al portapapeles. Fallback a "Copiar acceso a mi turno" + `navigator.clipboard.writeText()` + toast "Copiado ✓" cuando `navigator.share` no existe (detectado una sola vez al cargar la página).
3. ✅ **Botones de calendario unificados**: un solo botón "Agregar al calendario" abre un popup (reusa el estilo de `.confirm-overlay`) con "Google Calendar" y "Apple Calendar". Cada uno arma el link/`.ics` con un anchor sintético + `.click()` (mismo mecanismo que ya estaba probado, no navegación programática directa). El link de gestión del turno ahora se agrega en la descripción/notas de los dos ("Para cancelar o cambiar tu turno: ..."), así queda disponible en el evento del calendario aunque no se haya usado el botón de compartir. **La URL de Google Calendar se confirmó armando bien** fecha/hora (UTC-3 fijo, incluido el caso de cruce de día), título, ubicación y ahora el link — antes no estaba confirmado, ver detalle de prueba abajo.
4. ✅ **`/gestion`: badge Confirmado/Sin confirmar** de 8.5px/2px 7px a 9.5px/2.5px 8px (punto intermedio entre el tamaño grande original y el achicado de la vuelta pasada).
5. ✅ **`/gestion`: ícono de WhatsApp + lápiz** agrandado (ícono 28px→32px, SVG 14px→16px; lápiz 14px→16px, SVG interno 8px→9px), sin que se superpongan mal.

**Qué se pudo probar y qué no (este entorno no tiene Node/gh/Xcode completo, y el navegador de test no tiene acceso a internet — bloquea el CDN de intl-tel-input del que depende el resto del script de `/turnos`)**:
- ✅ Con una copia de prueba temporal (nunca commiteada, CDN reemplazado por un mock local para no depender de internet) se hicieron clicks reales: el popup de calendario abre y cierra bien, "Google Calendar"/"Apple Calendar" corren sin errores (la navegación externa a calendar.google.com quedó bloqueada por la política de red de este entorno de test, no por el código), "Copiar acceso a mi turno" copió al portapapeles y mostró el toast "Copiado ✓" correctamente (el navegador de test no tiene Web Share API, así que se probó el fallback, no el share nativo).
- ✅ Con un test aislado de las funciones puras (`limitesUTC`, `construirICS`, `armarLinkGoogleCalendar`) se confirmaron 3 casos de fecha/hora, incluido uno que cruza de día (22:00 ARG → 01:00 UTC del día siguiente) — todos correctos, y el link de gestión aparece bien en la descripción de ambos.
- ✅ Layout visual confirmado por captura de pantalla: texto rojo dentro de la tarjeta de confirmación, popup de calendario, botones de `/gestion` agrandados.
- ❌ **No se pudo probar `navigator.share()` real en un iPhone (Safari) ni un Android (Chrome)** — no hay Simulador de iOS disponible en esta Mac (falta Xcode completo, hace falta `sudo xcode-select -s /Applications/Xcode.app/Contents/Developer` con contraseña del usuario) ni emulador de Android. **Antes de confiar 100% en el botón "Guardar acceso a mi turno" en dispositivos reales, probarlo a mano** — si `navigator.share()` no se comporta como se espera en algún navegador, el fallback de copiar sigue funcionando igual.
- ❌ **No se pudo confirmar la descarga real del `.ics` en Apple Calendar** (abre un diálogo/pantalla nativa que este entorno no puede inspeccionar) — mismo tipo de limitación ya documentada para el botón viejo de Apple Calendar.

**Nuevo pedido (2026-08-12, quinta vuelta) — Mensaje de WhatsApp + DNI en turnos + cruce de fichas + reintentos/cron + formato de fichas, MERGEADO A MAIN SIN CONFIRMAR EN PREVIEW**: rama `fix/mensaje-recordatorio-whatsapp`. Este entorno no tuvo acceso a `gh`/Vercel API ni a Node para probar el preview o simular localmente (a diferencia de vueltas anteriores) — se avisó el riesgo explícitamente y el usuario pidió mergear igual. **Falta confirmar en producción real**: los 4 escenarios de prueba de este pedido (ver abajo) y setear `CRON_SECRET` en Vercel (sin eso el cron de salud queda inactivo, no rompe nada). Revisar con atención el próximo uso real del sitio.
1. ✅ **Mensaje de WhatsApp**: ya cumplía el texto pedido (venía de la vuelta anterior, sin commitear) — se confirmó contra el pedido nuevo palabra por palabra, sin cambios de código. El emoji 🤗 pedido de nuevo se dejó afuera de nuevo, mismo motivo ya documentado (corrupción en `wa.me`, ver `decisions.md`) — avisado explícitamente al usuario, no es un olvido.
2. ✅ **Campo DNI opcional en `/turnos`**: input nuevo `#f-dni` en el formulario público (`turnos/index.html`), solo dígitos (6-9), se limpia de puntos/espacios antes de mandar. Viaja en el body de `POST /api/reservar` (`dni`), se guarda como línea `DNI: ...` en la `description` del evento de Calendar (`crearTurno()` en `lib/googleCalendar.js`) junto a un helper nuevo `extraerDni()` simétrico a `extraerTelefono()`/`extraerMotivo()`. El formulario de `/gestion` (secretaria) **no se tocó**, tal como se pidió — es un archivo/formulario completamente distinto. Solo afecta turnos nuevos de acá en adelante, los turnos viejos simplemente no tienen esa línea.
3. ✅ **Cruce de "fichas con turno hoy" por niveles de confianza** (`pacientes/index.html`, `procesarPacientesDeHoy()` reescrita): 1) DNI exacto → match directo (usa el DNI nuevo del punto 2, cuando el turno lo tiene). 2) Nombre y apellido exactos normalizados → match directo (criterio que ya existía). 3) Mismo teléfono + nombre "parecido" (similitud por distancia de Levenshtein, umbral 0.45, cubre diminutivos tipo "Pepe"/"José") → sección nueva "¿Es este paciente?", chip con borde punteado dorado que **no abre directo**, necesita el toque explícito en el botón de confirmación. El teléfono de cada candidato se trae con un endpoint nuevo y liviano (`modo=telefono` en `api/gestion/pacientes.js`, lee una sola celda del Sheet — no la ficha completa), limitado a como mucho 2 candidatos por turno para no disparar fetches de más. 4) Nombre parecido sin teléfono ni DNI que lo respalde → sin match, queda para buscar a mano (no se adivina). Nada de esto autocompleta ni fusiona datos automáticamente.
4. De paso, se agregaron reintentos (`conReintentos`) a 4 llamadas de `moverPropio()` en `api/reservar.js` y a las 2 llamadas de `api/gestion/turnos-dia.js` que no los tenían — quedaban afuera del pase de reintentos de la vuelta anterior.

5. ✅ **Reintentos completos en `api/gestion/pacientes.js`** (Bloque 2): las ~25 llamadas a Sheets/Drive que quedaban sin `conReintentos` (crear/editar/fusionar fichas, movimientos, prestaciones, backup de emergencia) ahora lo tienen, call site por call site — nunca envolviendo el cliente completo. De paso, se agregó una verificación anti-duplicado en `crearPaciente()` (mismo criterio que `crearTurno`): si `drive.files.copy()` se reintenta porque el primer intento sí llegó a copiar pero la respuesta se perdió, se detecta por DNI después de copiar y se descarta la copia de más, quedándose con la primera.
6. ✅ **Cron de salud diario, 4:00 AM hora Argentina** (Bloque 2): `vercel.json` nuevo (`"0 7 * * *"`, Argentina es UTC-3 fijo sin horario de verano) apuntando a un `modo=healthcheck` nuevo dentro de `api/gestion/pacientes.js` (no un archivo nuevo, para no pasar el límite de 12 funciones — sigue en 12). Autenticado con `CRON_SECRET` (header `Authorization` que Vercel agrega solo a las invocaciones de cron), no con `GESTION_KEY` (evita tener que exponer esa clave en `vercel.json`, que va al repo). Prueba disponibilidad de Calendar y listado de fichas (Sheets/Drive) — ambas lecturas puras, sin crear ni tocar ningún dato real. Si falla, dispara `avisarFallo()`. **Falta setear `CRON_SECRET` en Vercel** (ver `CLAUDE.md`) para que quede activo.

7. ✅ **Formato de fichas (Bloque 3)**: mayúscula/minúscula tipo oración en Nombre/Apellido/Localidad/Domicilio/Obra social/Plan/Tratamiento ya estaba resuelto de antes (`CAMPOS_FICHA_MAYUSCULAS`), no hizo falta tocar nada — se confirmó revisando el código, no había ningún campo en mayúscula completa. **DNI con puntos de miles**: helper nuevo `formatearDni()` en `pacientes/index.html`, aplicado en los 4 lugares donde se muestra (tarjeta de la lista, encabezado de la ficha, encabezado del panel de obra social, encabezado del grupo de duplicados) — el valor guardado/comparado sigue siendo solo dígitos, esto es puramente de presentación. **Botones más grandes**: `.btn-obra-social` y `.btn-volver` (usado tanto en `#volver-lista` como `#volver-lista-desde-dup`), más padding y `font-size` de ~13px a 15px. **Estado financiero solo con Saldo**: Total y Pagado se ocultan con `class="hidden"` en `pacientes/index.html` — se siguen trayendo del backend y calculando igual que siempre (`api/gestion/pacientes.js` no se tocó), solo no se muestran. Saldo + el indicador "AL DÍA"/"DEBE" quedan visibles.

**Nuevo pedido (2026-08-12, cuarta vuelta) — Mensaje de recordatorio de WhatsApp reescrito, completo**: probado en rama separada (`fix/mensaje-recordatorio-whatsapp`).
1. ✅ **Plantilla nueva**, saludo neutral (ya no asume quién recibe el mensaje — el número guardado puede ser de un familiar, no siempre del paciente): "¡Hola, buenos días/buenas tardes!!! Te quería recordar el siguiente turno/sobreturno:" → nombre del paciente en su propia línea → "Hoy/Mañana/el X de mes a las Xhs" → línea de confirmación con el link → cierre de siempre. Ya no incluye "con Franco". Ver `decisions.md`.
2. ✅ **Corte de "buenos días"/"buenas tardes" cambiado de las 12hs a las 13hs** (`saludoActual()`), a pedido explícito.
3. ✅ **Emoji 🤗 pedido de nuevo, dejado afuera de nuevo** — re-comprobado empíricamente en esta misma vuelta (`curl` directo a un `wa.me/...?text=` real con 🤗 codificado): sigue corrompiéndose antes de llegar al chat. No es un olvido, ver `decisions.md`.

Probado en rama separada con datos simulados y la hora forzada vía query param (`?mockHour=N`, override de `Date`/`Intl` en un script de prueba, no en el código real) para no tener que esperar a que sea esa hora de verdad: `mockHour=10` y `mockHour=12` → "buenos días"; `mockHour=13` y `mockHour=15` → "buenas tardes" (corte exacto confirmado en el límite). Turno vs. sobreturno confirmado correcto en las dos líneas que lo mencionan (incluido `tipo=sobreturno` en el link). Cambio acotado a `gestion/index.html`, sin tocar `api/` ni clientes de Google.

**Nuevo pedido (2026-08-12, tercera vuelta) — Ajustes visuales de la fila en `/gestion` + fix del mensaje de WhatsApp, completo**: probado en rama separada + preview de Vercel (regla nueva, ver `CLAUDE.md`), no en producción directo.
1. ✅ **Badge "Sin confirmar"/"Confirmado" más chico** (`.confirmado-badge`): de 10.5px/padding 3px 9px a 8.5px/padding 2px 7px — mismos colores verde/rojo, más discreto al lado del resto de la fila.
2. ✅ **"Editar tel." integrado al botón de WhatsApp**: se sacó el botón "Editar tel." aparte; ahora hay un recuadro (`.wa-edit-wrap`, `box-shadow` alrededor del ícono verde de WhatsApp) con un lapicito chico superpuesto en la esquina (`.wa-edit-pencil`, ícono SVG nuevo `PENCIL_ICON`) que dispara la misma función de edición inline de siempre (`data-action="editar-tel"`, sin cambios de backend ni de `abrirAgregarTelefonoInline`).
3. ✅ **Mensaje de WhatsApp corregido**: el saludo repetía el nombre dos veces en la misma oración ("Hola ... Fausto Vinzon ... turno Fausto Vinzon con Franco..."). Ahora el saludo es genérico ("¡Hola, buenos días/buenas tardes!") y el nombre aparece una sola vez, en la parte que habla del turno.
4. ✅ **Regla nueva y permanente agregada a `CLAUDE.md`**: ningún cambio de código va directo a `main` — rama nueva → push → probar en el preview de Vercel de esa rama → recién ahí mergear a `main`. Motivo: el incidente de esta misma vuelta anterior (agenda y `/turnos` caídos en producción por un cambio nunca probado contra la API real). Ver `decisions.md`, "Incidentes en producción".

**Preview de Vercel de la rama confirmado desplegado** (`https://od-francovinzon-9ccktnd80-odfrancovinzon.vercel.app`, vía la API de GitHub — "Deployment has completed"), pero **no se pudo inspeccionar visualmente desde acá**: Vercel tiene protección SSO activada en los previews, cualquier request sin sesión de Vercel logueada redirige a `vercel.com/sso-api` (tanto páginas como `/api/*`) — no es un bug de este cambio. Verificación real hecha en cambio contra el código exacto de este commit corriendo local con datos simulados: badge más chico visible, lapicito abre la edición de teléfono sin disparar el link de WhatsApp, mensaje decodificado sin el nombre repetido. Cambio acotado a `gestion/index.html` (CSS/HTML/JS puro, sin tocar `api/` ni los clientes de Google) — no es la misma categoría de riesgo que el incidente de la vuelta anterior. Mergeado a `main` con el OK explícito del usuario tras explicarle la limitación de la protección SSO.

**Nuevo pedido (2026-08-12, segunda vuelta) — Link de confirmación en el WhatsApp + 4 botones en `/turno`, completo**:
1. ✅ **Link de confirmación en el mensaje de WhatsApp**: la nueva línea "Para confirmar, cancelar o cambiar el turno, entrá acá: [link]" se agregó al mensaje precargado del botón de WhatsApp de cada fila en `/gestion` (`mensajeRecordatorio()`), con el mismo link que ya usaba el QR del ticket térmico (`/turno?eventId=...`). Texto del mensaje reescrito según el pedido explícito (saludo dinámico buenos días/tardes, "con Franco", "Te esperamos") — revierte una decisión anterior, ver `decisions.md`. El emoji 🤗 pedido para el mensaje se dejó afuera a propósito por el bug ya comprobado de corrupción de emoji en `wa.me` (ver `decisions.md`), no por decisión propia — se avisa explícitamente, no es un olvido.
2. ✅ **Tres botones de acción en `/turno`** ("Confirmo el turno", "Cancelo el turno", "Reprogramo día y horario") — Confirmar es 100% nuevo (acción pública `confirmar` en `api/reservar.js`, sin restricción de fecha, disponible siempre incluso el mismo día); Cancelar/Reprogramar ya existían, solo se relabelearon a la nueva voz en primera persona.
3. ✅ **Botón "No estoy seguro"** — disclaimer + botón que reusa el flujo de reprogramar + nota y botón de WhatsApp para cuando ningún día/horario sirve.
4. ✅ **Indicador verde/rojo "Confirmado"/"Sin confirmar" en `/gestion`**, con toggle manual — no estaba pedido en el mensaje original de esta vuelta pero se agregó al toque siguiente del usuario ("agrega que el recuadro sea verde si ya lo confirmó y rojo si no"), ya que el pedido de arriba asumía que este indicador ya existía (no era así — es la primera vez que se implementa, ver `decisions.md` "Confirmación de turno").

Probado de punta a punta con servidor estático local + `fetch` mockeado (sin credenciales de Google, mismo motivo de siempre): en `/turno`, "Confirmo el turno" cambia a "✓ Turno confirmado" (deshabilitado) y muestra el toast; "No estoy seguro" despliega el disclaimer con el botón de reprogramar y el de WhatsApp; reprogramar de punta a punta (elegir día 20, horario 09:00, confirmar) funciona y resetea el botón de confirmar a su estado inicial; cancelar deja la pantalla de "Turno cancelado" limpia. En `/gestion`, logueado con datos simulados (un turno sin confirmar y un sobreturno confirmado): los badges verde/rojo se ven con los colores correctos, el link de WhatsApp decodificado tiene el texto y el link exactos esperados (incluido `tipo=sobreturno` para el sobreturno), y tocar el badge rojo lo cambia a verde con el toast correspondiente. No probado contra producción real (sin `GESTION_KEY` en esta máquina).

**Nota — este commit también sube trabajo de una vuelta anterior que había quedado sin commitear en el disco** (reintentos automáticos + alertas por email ante fallas reales, ver `lib/retry.js`/`lib/alertas.js` y `architecture.md`): se revisó el código, es acotado y consistente con el resto del proyecto (nunca reintenta errores "de verdad", nunca rompe un endpoint si falta `RESEND_API_KEY`), así que se subió junto con esta vuelta en vez de dejarlo pendiente indefinidamente. No fue parte del pedido de esta conversación.

## Hecho y desplegado — pedidos anteriores (no son pendientes, solo referencia)

**Nuevo pedido (2026-08-12, primera vuelta) — Cartel de respeto de horarios en `/turnos`, completo**: ✅ mensaje cálido en la pantalla de éxito (post-reserva/reprogramación/QR) invitando a cancelar o reprogramar con tiempo si no se puede asistir, con botón de WhatsApp para agendar el número del consultorio como canal de modificación/cancelación; más un cartel arriba del calendario avisando que si ningún día u horario sirve, se puede escribir por WhatsApp porque siempre se encuentra un lugar. Ver `changelog.md` para el detalle técnico.

**Nuevo pedido (2026-08-07, decimonovena vuelta) — 4 puntos independientes, los 4 completos**:
1. ✅ Ícono del chatbot: robot 🤖 — hecho, ver `changelog.md`. Reemplaza el ícono de burbuja SVG (fab) y 💬/✨ (encabezado) en los tres widgets (`index.html`, `/turnos`, `/gestion`). **Confirmado visualmente en producción en las 3 páginas.**
2. ✅ Markdown crudo en las respuestas (`**texto**`, guiones de lista sin procesar) — hecho, ver `changelog.md`. Dos capas: el system prompt de Gemini ahora pide texto plano explícitamente, y el frontend (los tres widgets) escapa el texto y convierte `**negrita**`/guiones a `<strong>`/viñeta real como respaldo. **Confirmado en producción** simulando una respuesta con markdown (la cuota gratis de Gemini estaba agotada por tanto testeo del día, ver nota abajo): se vio negrita real y viñetas, sin ningún asterisco ni guion crudo, tanto en `/gestion` como en `/turnos`.
3. ✅ Botón de enviar "no anda bien" — investigado a fondo en vivo (tap normal, doble tap rápido con mouse, doble Enter rápido): **no se pudo reproducir un envío duplicado** — el guard existente (`asistenteEnviando`) ya lo prevenía correctamente en todos los casos probados. Se aplicaron tres mejoras defensivas de todos modos: tap target más grande (36px→40px), `pointer-events: none` mientras está deshabilitado, y un guard contra `e.isComposing` (Enter disparado por un teclado predictivo/IME al confirmar una sugerencia, que podía enviar el mensaje a mitad de escribir — bug real de categoría conocida, aunque no se logró reproducir el síntoma exacto reportado). La sospecha principal quedó en el punto 4: el salto de layout probablemente hacía que el botón "se moviera" bajo el dedo justo al tocarlo.
4. ✅ Animación inestable del chat ("sube y baja") + radio de `/gestion` que no reproducía/no cambiaba de estación — hecho, ver `changelog.md`. La causa real del salto de layout, medida con precisión: el indicador de "escribiendo" vivía como hermano flex de `#asistente-messages` y le achicaba el alto disponible cada vez que aparecía (362px→378px al ocultarse, confirmado con `ResizeObserver`+polling) — se movió adentro del contenedor scrolleable. La radio no cambiaba de estación porque el fundido de 3s+3s agregado en la vuelta anterior (a pedido explícito en su momento) se sentía, en la práctica, como que "no respondía" — se sacó y quedó el cambio instantáneo que se pidió ahora. **Las dos cosas confirmadas en producción**: la radio cambia de estación al instante (medido con `readyState`/`currentTime` del `<audio>`, no solo visualmente) y pausa correctamente al tocar la misma que está sonando; el alto del área de mensajes del chat se mantuvo constante (355px) mandando una respuesta simulada, sin el salto de antes.

**Nota sobre verificación**: para este pedido se pidió y se usó la `GESTION_KEY` para probar `/gestion` en vivo (algo que no había sido posible en vueltas anteriores) — los puntos de `/gestion` de esta vuelta y de la anterior (radio, ícono, fallback del asistente) quedan confirmados visualmente, no solo por revisión de código. La cuota gratis de Gemini (`gemini-3.6-flash`) se agotó durante el testeo de hoy (error 429 "quota exceeded", visto varias veces) — es un límite externo del plan gratuito, no un bug; el fallback honesto (ver vuelta anterior) se activó correctamente en todos esos casos.

Último pedido grande (2026-08-06) traía 8 puntos. Quedan implementados el 1, 2, 3, 5, 6 y 8. El 4 (feriados) y el 7 (Liquid Glass específicamente, distinto del pulido general de microinteracciones ya hecho) siguen pendientes.

**Nuevo pedido grande (2026-08-06, decimosexta vuelta) — 4 secciones independientes, las 4 completas**:
1. ✅ Animaciones en `/turnos` — hecho, ver `changelog.md`.
2. ✅ Botones de copiar link en `/gestion` — hecho, ver `changelog.md`.
3. ✅ Chatbot público (web + `/turnos`) con Gemini, reservando turnos conversando — hecho, ver `changelog.md`. Fue la pieza más grande de las 4, con una decisión de alcance importante (ver nota abajo).
4. ✅ Radio en vivo en `/gestion` — hecho, ver `changelog.md`.

**Nota sobre la sección 3** (para que quede claro, tal como se pidió avisar si resultaba más compleja de lo esperado): terminó implementada completa — backend con function-calling de Gemini, reserva conversacional de punta a punta, y el widget en las 3 páginas — pero fue efectivamente la parte más grande del pedido. Se tomó una decisión de alcance deliberada: el teléfono en la reserva por chat se pide como texto libre con código de país explícito (ej. "+54 9 3442..."), en vez de embeber el selector de país visual (`intl-tel-input`) dentro de la conversación como tienen los formularios clásicos — eso hubiera sumado bastante más trabajo de UI a una sección ya grande. La validación "no adivinar el país" se mantiene igual (se rechaza un teléfono sin código de país explícito), solo cambia que no hay selector visual dentro del chat. Si se quiere ese nivel de pulido, es una mejora futura acotada.

**Nuevo pedido (2026-08-07, decimoséptima vuelta) — 2 secciones independientes, las 2 completas**:
1. ✅ Ticket térmico 58mm en `/gestion` (impresora HPRT TP585L) — hecho, ver `changelog.md`. Encontró y corrigió de paso un bug preexistente ("Turno cancelado" invisible en `/turnos`).
2. ✅ Reproductor de radio en `/gestion` con 5 estaciones (Radio Mitre, La 100, Aspen, FM Milenium, Radio Rivadavia) — hecho, ver `changelog.md`. Se investigaron las 7 pedidas; **El Observador y streAM 950 se sacaron de la lista a pedido explícito** después de evaluarlas (El Observador solo tenía un iframe de video de Kick.com como alternativa, sin audio propio; streAM 950 no tiene ninguna fuente en vivo todavía — ver `changelog.md` para el detalle de cada una).

**Nuevo pedido (2026-08-07, decimoctava vuelta) — 7 puntos independientes, los 7 completos**:
1. ✅ Bug urgente: el chatbot no respondía ("No se pudo consultar al asistente") — hecho, ver `changelog.md`. Causa raíz encontrada en los logs de Vercel: Google restringió `gemini-2.5-flash` para API keys nuevas (`gemini-2.5-flash` sigue GA en general, pero devuelve 404 "no longer available to new users" para keys creadas después de cierta fecha). Se cambió a `gemini-3.6-flash` en `api/chat.js` y `api/gestion/asistente.js`. **Probado con una pregunta real en producción** (pedido explícito antes de dar nada por terminado): "hola, no puedo sacar un turno, me ayudás" en `/turnos` respondió coherente y ofreció ayudar a reservar. El de `/gestion` recibió el mismo fix de código pero no se pudo probar en vivo — no hay `GESTION_KEY` disponible en esta máquina para loguearse.
2. ✅ Fallback honesto si el asistente falla (cualquiera de los dos, por cualquier motivo) — hecho, ver `changelog.md`. Mensaje "No puedo resolver tu consulta en este momento. Lo mejor es que te comuniques por WhatsApp." con botón directo al WhatsApp real del consultorio, en vez de un texto de error genérico o fallar en silencio. Aplica a los tres widgets de chat (`index.html`, `/turnos`, `/gestion`).
3. ✅ Auto-scroll en `/turnos`: al tocar un día, scroll suave a los horarios; al elegir un horario, scroll suave al formulario — hecho, ver `changelog.md`. **Probado en vivo en producción** con una reserva real de principio a fin.
4. ✅ Animaciones de aparición en `/turnos`: horarios con fade + leve movimiento y desfasaje mínimo entre botones; formulario con deslizamiento hacia abajo (revelándose detrás de los horarios) — hecho, ver `changelog.md`. **Probado en vivo en producción**, mismo turno de prueba del punto 3.
5. ✅ Reproductor de radio de `/gestion` rediseñado como tarjeta — hecho, ver `changelog.md`. Se saca el ícono flotante chico (mismo patrón que tenía el asistente hasta ahora, pero para la radio no correspondía más) y se reemplaza por una tarjeta bien visible dentro del sidebar (desktop) y debajo del resumen (mobile). Lista final: Radio Mitre, La 100, Aspen, Radio Rivadavia y **Jazz 24hs — Blackie FM** (nueva, reemplaza a FM Milenium que se sacó a pedido explícito) — el stream real de Blackie se encontró inspeccionando el sitio no oficial `blackiefm.com.ar` (que tiene banners publicitarios engañosos, ignorados) y es la misma infraestructura StreamTheWorld que las otras cuatro, confirmada con `curl` (`icy-name: Blackie`, `icy-genre: Classic Jazz`). ~~Transición entre radios con fade out de 3s + fade in de 3s~~ — **actualización (2026-08-07, decimonovena vuelta): se sacó el fundido**, se sentía como que la radio "no respondía" (varios segundos sin cambio audible/visible al tocar otra estación) — ver ese pedido más arriba. Cambio de estación instantáneo ahora, confirmado en producción con `GESTION_KEY` real: la radio reproduce al tocar play, cambia de estación al instante (pausa la anterior, arranca la nueva), y pausa correctamente la que está sonando.
6. ✅ Página propia para el QR del ticket impreso (`/turno`, distinta de `/turnos`) — hecho, ver `changelog.md`. Título "Detalles de tu turno" (no "Reservar turno"), logo, nombre del paciente, día/horario en un renglón dorado propio, y las mismas acciones de modificar/cancelar reusando `api/reservar.js` (`obtener`/`mover`/`cancelar`) sin duplicar lógica de negocio — sí se duplicó el HTML/CSS/JS del calendario/horarios/reprogramar (mismo patrón ya usado en el proyecto para `normalizarTelefonoWhatsApp()`, sin bundler para compartir código entre páginas). El QR de `gestion/index.html` ahora apunta a `/turno?eventId=...` en vez de `/turnos?eventId=...`. **Probado de punta a punta en producción** con un turno de prueba real (creado, reprogramado dos veces, consultado vía `/turno`, y cancelado desde ahí mismo) — encontró y corrigió de paso un bug real preexistente (ver ítem separado abajo). El punto de entrada viejo (`/turnos?eventId=`) se dejó intacto por compatibilidad con tickets ya impresos, no se tocó.
7. ✅ Impresión del ticket térmico estrictamente en blanco y negro — hecho, ver `changelog.md`. El texto y los fondos ya se forzaban a negro/transparente sin sombra; se agregó además `text-shadow: none` explícito y, sobre todo, un filtro de alto contraste (`grayscale(1) contrast(1000%)`) sobre el logo — es el único elemento con semitonos reales (antialiasing del PNG), el QR ya se genera con colores puros (`#000000`/`#ffffff`). **No probado contra la impresora física** — no hay forma de interactuar con una impresora térmica real ni con el diálogo nativo de impresión del SO desde este entorno. Ver nota de verificación floja abajo.

**Bug real encontrado y corregido al probar el punto 6 de punta a punta**: un turno recién cancelado podía seguir apareciendo como válido al consultarlo por `accion: 'obtener'` — `events.get` de Google Calendar no siempre tira error para un evento recién borrado, puede devolver el mismo recurso con `status: 'cancelled'` (tombstone) en vez de un 404 inmediato, y `obtenerPropio()` en `api/reservar.js` no chequeaba ese campo. Se agregó el chequeo (`ev.status === 'cancelled'` → tratar como "no encontrado", mismo mensaje que el catch). Afecta también al punto de entrada viejo `/turnos?eventId=`, no solo a `/turno` nuevo. **Confirmado el fix en producción**: el mismo `eventId` que antes seguía devolviendo los datos después de cancelado, ahora muestra "No encontramos ese turno. Puede que ya haya sido cancelado." Ver `changelog.md`.

## Hecho y desplegado (no son pendientes, solo referencia)

- ✅ **Reproductor de `/gestion` con radios específicas** (2026-08-07, decimoséptima vuelta, sección 2/2): Radio Mitre, La 100, Aspen, FM Milenium, Radio Rivadavia — stream de audio directo cada una, URLs investigadas a mano en cada sitio oficial y confirmadas con `curl`. El Observador (solo tenía disponible un embed de video de Kick.com, sin audio propio) y streAM 950 (sin ninguna fuente en vivo todavía) se evaluaron y se sacaron a pedido explícito. Probado en vivo: las 5 reproduciendo audio real, cambio entre estaciones corta la anterior, sigue sin cortarse con el auto-refresh. Ver `changelog.md`.

- ✅ **Ticket térmico 58mm en `/gestion`** (2026-08-07, decimoséptima vuelta, sección 1/2): ícono de impresora en cada fila (turno/sobreturno), previsualización propia con estilo navy/dorado, `window.print()` con `@media print` recién al tocar "Imprimir" adentro. Ticket con logo real, nombre, fecha/hora, QR (`qrcodejs` vía CDN) y WhatsApp del consultorio. El QR reusa la pantalla de "Cambiar día y horario / Cancelar turno" de `/turnos` con un nuevo punto de entrada `?eventId=` — `api/reservar.js` sumó `accion: 'obtener'` fusionada (ya estábamos en 12/12) y `calendarId` whitelisteado para que sobreturnos también tengan QR funcional (sin "Cambiar día y horario" ahí, decisión de alcance — ver `changelog.md`). De paso se encontró y corrigió un bug preexistente: la pantalla "Turno cancelado" quedaba invisible (`opacity:0` sin `.card-visible`). Probado con datos simulados — no probado contra la impresora física ni el diálogo nativo real del navegador, ver nota de verificación floja abajo.

- ✅ **Radio en vivo en `/gestion`** (2026-08-06, decimosexta vuelta, sección 4/4): botón flotante discreto abajo a la izquierda con 3 radios argentinas (Radio Mitre, La 100, Metro vía StreamTheWorld), play/pausa. El `<audio>` vive fuera de `#app` (hermano del asistente y los overlays), así que el auto-refresh de 60s no puede cortarlo — confirmado en vivo dejándolo sonando más de 60 segundos reales sin cortes. Se pausa solo al cerrar sesión. Ver `changelog.md`.

- ✅ **Chatbot público con Gemini (web + `/turnos`)** (2026-08-06, decimosexta vuelta, sección 3/4): `api/chat.js` nuevo, con function-calling de Gemini reusando `lib/googleCalendar.js` (mismo cálculo de disponibilidad y creación de turno que ya usan `api/disponibilidad.js`/`api/reservar.js`, sin duplicar lógica). Fusión de `disponibilidad-mes.js` + `horarios-dia.js` en `api/disponibilidad.js` para liberar cupo (queda en 12 de 12 funciones, sin margen). Ventana de chat flotante en `index.html` y `turnos/index.html`, con cartel de bienvenida + confeti dentro del chat para reservas de paciente nuevo, y animación de burbujas estilo iMessage (aplicada también al chat ya existente de `/gestion`). Tope de 30 mensajes por conversación. Probado con datos simulados — ver `changelog.md` para el detalle y la decisión de alcance sobre el teléfono (texto con código de país, sin selector visual embebido).

- ✅ **Botones "copiar link" en `/gestion`** (2026-08-06, decimosexta vuelta, sección 2/4): "Copiar link de turnos" y "Copiar link de la web" debajo del bloque "Hola, Ayelen" (desktop y mobile), con fallback a `execCommand('copy')` si la Clipboard API no está disponible. Probado con click real de usuario — ver `changelog.md`.

- ✅ **Animaciones en `/turnos`** (2026-08-06, decimosexta vuelta, sección 1/4): slide sutil del calendario al cambiar de mes o de día seleccionado, entrada con fade+slide del panel de horarios, logo más chico (340px→280px), encabezado "Reservá tu turno" más grande (22px→27px), auditoría de hover/click en todos los botones (solo faltaba uno, en el diálogo de cancelar turno), confeti de paciente nuevo subido de 42 a 58 piezas. Probado en vivo con datos simulados — ver `changelog.md`. No probado contra producción.

- ✅ Ítem 1 — Botón "Mover": ícono cambiado a 🔄, permite editar motivo desde el mismo modal (turno y sobreturno). Desplegado y funcionando.
- ✅ Ítem 2 — Teléfonos: normalización compartida, WhatsApp con ícono real y mensaje dinámico, teléfono inválido = "sin teléfono", "Editar tel." permite vacío. Desplegado, probado en vivo con 5 formatos distintos (con 15, con 0, con espacios, con guiones, con +54) — los cinco normalizan igual. Bug de colisión con código de país de España (34) encontrado y corregido en la misma vuelta.
- ✅ **Selector de país en vez de adivinar el código de área** (2026-08-06): `intl-tel-input` en los 4 campos de teléfono nuevo, `telefonoParaWhatsApp()` a partir del E.164 real. Ver `decisions.md`.
- ✅ **Ítem 3 — Lista de tareas inteligente en el sidebar** (2026-08-06): implementada. Ver detalle abajo — ya no es un pendiente.
- ✅ **Tres correcciones sobre lo anterior** (2026-08-06, misma tarde): (1) el badge de la fila pasó de "cualquier teléfono sin `Teléfono verificado: Sí`" a "⚠ Tel. inválido" solo cuando el teléfono guardado no pasa la validación real de formato — un legado válido ya no se marca. (2) La lista de tareas del sidebar sacó la categoría "confirmar teléfono"; ahora son solo "Agregar teléfono" (sin ningún teléfono cargado) y "Mover N turnos" (día bloqueado por completo que todavía tiene turnos asignados, botón "Ir al día") — ver `buscar.js?modo=tareas`. (3) Se sacaron los emoji (😊🦷) del mensaje precargado de WhatsApp: comprobado que `wa.me`/`api.whatsapp.com` los corrompe (los reemplaza por "�" ya en el propio link hacia `web.whatsapp.com`, antes de llegar al chat) — no era un bug de nuestro `encodeURIComponent`. Las tres correcciones probadas en vivo con datos simulados. Ver `decisions.md`.
- ✅ **Ítem 5 — Agregar al calendario del dispositivo** (2026-08-06, cuarta vuelta): botón en la pantalla de confirmación de `/turnos`, genera un `.ics` al vuelo (sin librerías) con la fecha/hora en UTC correcta (Argentina −03:00). Ver `decisions.md`.
- ✅ **Ítem 6 — Modificar/cancelar turno desde la confirmación** (2026-08-06, cuarta vuelta): implementado con alcance reducido a propósito respecto de lo que decía este ítem — ver `decisions.md`, "Modificar/cancelar turno desde la confirmación: sin link persistente". Sin login ni token en URL: el `eventId` que devuelve `/api/reservar` al crear vive solo en memoria del cliente durante esa sesión/carga de página, y solo sirve para mover/cancelar ESE turno (nunca para listar ni para acceder después de recargar). `reservar.js` ahora acepta `accion: 'mover'|'cancelar'` además de crear (default).
- ✅ **Ítem 8 — Toasts + animaciones de feedback** (2026-08-06, cuarta vuelta): toasts para las 4 acciones importantes (turno creado/reprogramado/eliminado, cambios guardados), diálogo de confirmación propio (reemplaza `confirm()` nativo en cancelar turno y desbloquear día), resaltado breve de la fila creada/reprogramada, y actualizaciones locales sin refetch para editar teléfono y cancelar (sin parpadeo de "Cargando..."). Además, sin pedirlo como ítem separado: buscador reposicionado arriba del sidebar, auto-retorno a la agenda al vaciar el buscador (se sacó el botón "Volver a la agenda"), y "Nuevo turno" en `/gestion` suma la casilla "Paciente nuevo" igual que `/turnos`. Ver `decisions.md` y `changelog.md`.
- ✅ **Botón cancelar (✕) en "Agregar/Editar teléfono" inline de `/gestion`** (2026-08-06, sexta vuelta): el selector de país (`intl-tel-input`) para ese campo ya estaba implementado desde la "segunda vuelta" — lo que faltaba era poder cerrar el editor sin guardar. Ahora `abrirAgregarTelefonoInline` muestra ✕ (Cancelar) junto a ✓ (Guardar); ✕ no llama al backend, solo descarta la instancia de `intl-tel-input` y vuelve a `renderAgenda()` con `agendaItems` intacto (que nunca se tocó), así que la fila vuelve exactamente a como estaba: con su teléfono previo si tenía uno, o vacía si no. Probado en vivo (servidor estático local + `fetch` mockeado, sin credenciales de Google): editar el teléfono de un turno con número existente y cancelar (el número y el link de WhatsApp quedan intactos, sin request a `agregar-telefono`), y agregar teléfono a un turno sin ninguno y cancelar (vuelve a "+ Agregar teléfono", sin request). También se confirmó que "Guardar" (✓) sigue funcionando sin romperse.
- ✅ **"Reorganizar turnos" extendido a horarios parciales + prioridad fija en el sidebar** (2026-08-06, séptima vuelta): la tarea que antes solo cubría días bloqueados por completo (`bloqueo-dia.js`) ahora también se genera para bloqueos de rango horario puntual (`bloquear-horario.js`), con una tarea POR CADA bloqueo (no por día) y el solapamiento calculado contra el rango real del bloqueo, no por "mismo día calendario" — así un turno fuera del rango bloqueado no cuenta. Texto: "Reorganizar turnos del [fecha]", botón "Ir al día". Backend: `buscar.js?modo=tareas` renombró `diasBloqueados` a `reorganizar` (ver `architecture.md`). Frontend: las tareas de "Reorganizar turnos" ahora se ordenan siempre antes que las de "Agregar teléfono" (dos grupos separados, cada uno ordenado por fecha adentro, en vez del orden cronológico único de antes). Probado en vivo con datos simulados (servidor estático local + `fetch` mockeado, sin credenciales de Google): (1) bloqueo de día completo con 2 turnos → tarea con "2 turnos", "Ir al día" navega bien; (2) bloqueo de horario parcial (14–16h) con 1 turno adentro (14:30) y 1 turno afuera (09:00) el mismo día → tarea con "1 turno" (cuenta correctamente solo el que se solapa), "Ir al día" navega bien; (3) orden confirmado: las dos tareas de reorganizar (7 y 10 de agosto) aparecen antes que la de "Agregar teléfono" (8 de agosto), pese a que cronológicamente el 8 va en el medio. También se validó por separado la lógica de solapamiento (`t.start < b.end && t.end > b.start`) con datos de prueba en consola antes de mockear el endpoint completo. No probado contra el Calendar real en producción.
- ✅ **Resumen compacto en mobile + modal de tareas + destacado "urgente" para "Reorganizar turnos"** (2026-08-06, octava vuelta): en `/gestion` el bloque de saludo/turnos hoy/huecos libres antes solo existía en el sidebar de desktop (≥1000px) — no se veía nada de eso en mobile. Ahora hay una franja compacta `.mobile-summary` arriba de la agenda (misma identidad visual: mismos títulos/colores/tipografía) con saludo, fecha, turnos hoy, huecos libres y próximo bloqueo, más un botón "Tareas pendientes (N)" (oculto si no hay tareas) que abre un modal con la lista COMPLETA de tareas (sin el tope de 6 del sidebar), mismo contenido/orden/botones que en desktop. Desktop (≥1000px) no se tocó — la única diferencia visible ahí es el nuevo punto de abajo. Además, en ambos lugares (sidebar desktop y modal mobile), las tareas "Reorganizar turnos" ahora se destacan como urgentes: recuadro con fondo rojo suave (`rgba(193,68,55,.07)`, no saturado) + ícono ❗, para que se resuelvan antes que "Agregar teléfono" (que queda con el estilo neutro de siempre). Ver `decisions.md`. Probado en vivo con datos simulados en viewport mobile (375×812) y desktop (800×450): el bloque compacto se ve completo y prolijo sin ocupar media pantalla, el botón muestra el conteo correcto (3), el modal abre con las 3 tareas en el orden correcto y el destacado rojo en las 2 de "Reorganizar turnos", "Ir al día" cierra el modal y navega al día correcto, el botón ✕ y el click en el fondo también cierran el modal, y en desktop el sidebar se ve exactamente igual que antes salvo el destacado rojo (confirmado con captura de pantalla). Ajuste encontrado en el camino: el título "Reorganizar turnos del [fecha]" se truncaba con "…" en el sidebar angosto de desktop (heredaba `white-space:nowrap` de los nombres de pacientes) — se corrigió para que haga wrap a dos líneas en vez de perder la fecha.
- ✅ **Pantalla de confirmación de turno en `/turnos`: texto resaltado, calendario arreglado, animaciones y confeti** (2026-08-06, novena vuelta) — 5 puntos:
  1. **"Turno confirmado para..." resaltado**: más grande (17px), negrita, verde "confirmado" (`--confirmado: #4F9A3C`, nueva variable, distinta de `--success` que ya usaba el ícono ✓ y de `--gold`). Aplica tanto a `#success-text` (paciente recurrente) como `#success-text-nuevo` (paciente nuevo), mismo mensaje del backend en los dos casos.
  2. **"Agregar al calendario" dividido en dos botones**: "📅 Agregar a Apple Calendar" (mismo `.ics` generado en el cliente, pero ahora SIN atributo `download` — se navega directo al blob `Content-Type: text/calendar`, que en teoría es lo que hace que iOS Safari abra la pantalla nativa "Agregar evento" en vez de forzar una descarga a la app Archivos) y "📅 Agregar a Google Calendar" (link directo a `calendar.google.com/calendar/render?action=TEMPLATE&...`, sin ningún archivo de por medio, abre en pestaña nueva). Los dos comparten los mismos datos del turno (`limitesUTC()` centraliza el cálculo UTC±3 usado por ambos). **No probado en iPhone real ni en Simulador — ver nota abajo, es el punto más importante a confirmar.**
  3. **Transición de entrada más suave**: fade + leve movimiento (`opacity`/`translateY`, 220ms `ease`, mismo timing/curva que el resto del sitio) al pasar del formulario a la pantalla de éxito, en vez del aparecer/desaparecer instantáneo de `.hidden`. Aplica a ambas pantallas de éxito y también a la de "turno reprogramado".
  4. **"¡Bienvenido/a!" con rebote sutil**: zoom-in con `cubic-bezier(0.34, 1.56, 0.64, 1)` (easeOutBack, overshoot natural sin necesitar keyframes multi-paso) cada vez que se muestra la pantalla de bienvenida de paciente nuevo.
  5. **Confeti para paciente nuevo**: ráfaga de 26 piezas cayendo desde arriba, colores navy/dorado/blanco, ~1-1.5s, `pointer-events: none` (nunca bloquea los botones), se limpian del DOM solas.
  - Probado en vivo con datos simulados (servidor estático local + `fetch` mockeado, sin credenciales de Google en esta máquina): los 5 puntos verificados en el navegador — texto verde/negrita/grande confirmado visualmente, transición de entrada capturada en pleno movimiento (opacity parcial) en captura de pantalla, `href` de ambos botones de calendario verificados por código (`.ics` con fechas UTC correctas, `dates=` de Google Calendar coincidiendo exactamente con el `.ics`, sin atributo `download` en el link de Apple), rebote de "¡Bienvenido/a!" confirmado con `getAnimations()` (animación `bienvenidoPop` corriendo) y visualmente en captura, confeti confirmado por código (26 piezas `.confetti-piece` presentes a los 350ms, se auto-eliminan a los 2s).
  - **Punto 2 (iPhone real) NO verificado**: esta máquina no tiene Xcode completo instalado (solo Command Line Tools), así que no se pudo abrir el Simulador de iOS, y no hay un iPhone físico disponible acá. El fix se implementó exactamente como se pidió (sin `download`, navegación directa a blob `text/calendar`), pero el comportamiento real de iOS Safari con esta técnica queda sin confirmar. **Pendiente: probar "Agregar a Apple Calendar" en un iPhone real.** Si sigue yendo a Archivos en vez de abrir Calendar directamente, sacar ese botón y dejar únicamente "Agregar a Google Calendar" (ya implementado y funcionando, sin esta incertidumbre).
- ✅ **Cinco arreglos chicos en `/gestion`** (2026-08-06, décima vuelta): ícono de cancelar turno cambiado de 🗑 emoji a un tacho de contorno estándar (`TRASH_ICON`, reusado en las dos filas); sidebar de desktop ensanchado 340px→390px para que "Reorganizar turnos del [fecha]" entre en una línea; auto-refresh de la agenda cada 60s que se salta el ciclo entero si hay un panel/modal abierto o un input con foco (`haySesionOcupada()`); y el bug real de que "Reorganizar turnos" no aparecía para bloqueos cargados con mucha anticipación — la ventana de detección de bloqueos en `buscar.js?modo=tareas` era de solo 14 días (la misma que "Agregar teléfono"), se separó en dos ventanas: `sinTelefono` sigue en 14 días, `reorganizar` ahora en 120 (mismo horizonte que `proximo-bloqueo.js`). Ver `changelog.md` y el punto siguiente sobre el script de migración de motivo.

- ✅ **Migración de motivo en título corrida y confirmada** (2026-08-06, undécima vuelta): `api/gestion/migrar-motivo-titulo.js` se disparó contra producción (45 eventos migrados en ambos calendarios, `CALENDAR_ID` y `SOBRETURNOS_CALENDAR_ID`, confirmando que sí cubría los dos). Caso puntual pedido por el usuario verificado: "Axel Martin (se le salio brackets)" (sobreturno) quedó con título "Axel Martin" y `Motivo: se le salio brackets`, confirmado leyendo el evento real vía `buscar.js`. **Bug real encontrado y corregido en la misma corrida**: 3 de los 45 eventos tenían el patrón viejo `"Turnos (Nombre)"` de la limpieza de Apps Script (`decisions.md` — "Turnos" era un prefijo genérico, no un nombre), que el regex de la migración no distinguía de `"Nombre (motivo)"` y les invirtió título/motivo (quedaron con título "Turnos"/"Turnos Odontólogo Franco Vinzón" y el nombre real del paciente en `Motivo:`). Se agregó un modo `accion: 'revertir-turnos-genericos'` al mismo script con los 3 `eventId` exactos, se corrió una vez, y se confirmaron los tres reventidos correctamente (títulos "Nicolas Gomez", "Cecilia Romani", "Camila Daniela Rodriguez", con el motivo real original de cada uno intacto — dos de ellos tenían el motivo real en formato HTML viejo `<b>Motivo</b>\nX`, no tocado por el revert). Script borrado del repo tras confirmar todo — proyecto de vuelta en 11 de 12 funciones serverless.

- ✅ **Tres cambios chicos** (2026-08-06, duodécima vuelta): confeti de bienvenida con más partículas y un poco más de duración (26→42 piezas, ~0.9-1.5s→~1.1-1.9s, confirmado por conteo real en el DOM: 42 piezas en vuelo); navegación día por día en `/gestion` salta sábado/domingo (`cambiarDia()`, solo para pasos de ±1 día, no en vista semana) — confirmado en los dos sentidos: viernes 7/8→siguiente día da lunes 10/8, y lunes 10/8→día anterior vuelve al viernes 7/8; y "Mover" (tanto el flujo simple de sobreturnos como el de calendario para turnos) ahora permite corregir Nombre/Apellido — precarga con el nombre actual, valida que no quede vacío, y lo manda a `evento.js` que actualiza el título del evento (`"Nombre Apellido"`) junto con la fecha/hora/motivo. Confirmado en los dos flujos con datos simulados: el payload enviado al backend incluye el nombre corregido, y la validación de nombre vacío bloquea el envío sin llamar al backend.

- ✅ **Toasts en `/turnos` al cancelar/reprogramar** (2026-08-06, decimotercera vuelta): antes cambiar día/horario o cancelar el turno desde la pantalla de confirmación no avisaba con ninguna animación. Se reusó tal cual el componente `toast()`/`#toast-container`/`.toast` que ya existía en `/gestion` (mismo CSS, mismo JS, ver `changelog.md`) — "✅ Turno cancelado correctamente" al eliminar, "✅ Turno modificado correctamente" al cambiar día/horario. Confirmado con datos simulados: el texto exacto de cada toast aparece en el DOM apenas se completa cada acción (una sola instancia, sin duplicados).

- ✅ **Sección "Horarios de atención" en la home pública** (2026-08-06, decimocuarta vuelta): nueva sección entre el bloque de botones/WhatsApp/dirección y "Tratamientos", con los horarios reales de `WEEKLY_SCHEDULE` (`lib/googleCalendar.js`) día por día (Lunes a Domingo, martes con los dos rangos, sábado/domingo "Cerrado" en cursiva) en una tarjeta navy/dorado consistente con el resto del sitio, más una frase cálida invitando a acercarse en esos horarios. Revisado visualmente en desktop y mobile (390px) — bien ubicada, prolija, sin romper el layout.

- ✅ **Asistente de ayuda con IA (Gemini) en `/gestion`** (2026-08-06, decimoquinta vuelta): ventana de chat flotante (botón circular abajo a la derecha, abre/cierra un panel con historial de mensajes + input + botón enviar) para que Ayelen pregunte cómo usar el panel. Backend nuevo `api/gestion/asistente.js` — se contaron las funciones existentes bajo `api/` antes de crearlo (11 de 12, no se llegó al límite) y se sumó el archivo nuevo en vez de fusionar; **el proyecto queda ahora en 12 de 12, sin margen** — el próximo endpoint que se necesite va a requerir fusionar con uno existente (ver `CLAUDE.md`). Usa Gemini (`gemini-2.5-flash`, REST) server-side con `process.env.GEMINI_API_KEY`, con el `system_instruction` fijo pedido por el usuario (contexto completo del panel) y el historial de la conversación (en memoria del cliente, se recorta a los últimos 10 turnos). Estilo visual navy/dorado consistente con el resto de `/gestion`, mismas animaciones (fade + scale como `confirm-overlay`). Probado con servidor estático local y un mock del endpoint (sin `GEMINI_API_KEY` real en esta máquina): las 3 preguntas típicas pedidas responden con pasos numerados bien formateados, una pregunta fuera de tema dispara el rechazo amable, toggle abrir/cerrar y layout mobile (390px) verificados. Ver `decisions.md` y `changelog.md`.

## Módulo de Fichas de pacientes (Fase 1) — 2026-08-11

Módulo nuevo `/pacientes` (búsqueda en tiempo real, crear paciente, ver/editar ficha con autoguardado por campo, movimientos con Estado financiero, sincronización con el Google Sheet real de cada paciente) más el botón "Acceder a fichas" en `/gestion`. Alcance explícitamente excluido de esta fase: validación de duplicados/DNI, panel de administración configurable, auditoría — quedan para Fase 2.

- ✅ **Arquitectura de acceso a Sheets/Drive**: se descubrió en el camino que la cuenta de servicio (`turnos-bot@...`, la misma que usa Calendar) no tiene cuota de almacenamiento propia y no puede crear archivos en un Drive personal (no-Workspace) aunque la carpeta esté compartida como Editor — confirmado con una prueba real de API, no en la documentación. Se le presentaron las opciones al usuario y se eligió **OAuth como el odontólogo + selector de carpeta (Google Picker)**, con scope `drive.file` (no sensible, sin necesitar verificación de Google) en vez de un scope amplio de Drive. Ver `decisions.md` para el detalle completo. Calendar sigue con la cuenta de servicio, sin cambios.
- ✅ **Endpoint único `api/gestion/pacientes.js`** con todas las acciones (`listar`, `ficha`, `modified`, `telefono-turnos`, `crear`, `actualizar-campo`, `movimiento-agregar/editar/anular`) para no pasarse del límite de 12 funciones serverless — el proyecto ya estaba en 12/12, así que además se fusionó `proximo-bloqueo.js` dentro de `buscar.js` para liberar un lugar.
- ✅ **Autocompletado de teléfono desde turnos** (usando la cuenta de servicio de Calendar, sin scope nuevo): probado en producción con nombres reales de turnos ya cargados — coincidencia única devuelve el teléfono (`Santiago Bertti`, `Mia Gay`, `Patricia Rodriguez`, `Sebastian Oyarbide`, `Agustina Soleti`, `Juan Carlos Zapata`, `Sofia Perdomo`, todos `multiple:false` con el teléfono correcto) y coincidencia múltiple no ofrece autocompletar (`Angel Leguizamon`, que tiene más de un turno con teléfonos distintos, devolvió `multiple:true` correctamente). El popup "¿El número de teléfono de [nombre] es [teléfono]?" con Sí/No se probó en la interfaz real (`/pacientes`, paciente de prueba "Santiago") y funciona exactamente como se especificó, incluyendo que "Sí" completa el campo Teléfono del formulario.
- ✅ **Bug real encontrado y corregido: typo en la Picker API Key.** La clave estaba mal transcrita en `gestion/conectar-drive.html` (una `l` minúscula en vez de una `I` mayúscula, dos caracteres visualmente idénticos en la mayoría de las fuentes) — causaba "The API developer key is invalid." al abrir el selector de carpeta. Se comparó la clave real (revelada en la consola de Google Cloud) contra la hardcodeada y se encontró la diferencia exacta. Corregido y confirmado: el Picker ahora abre correctamente sin error.
- ✅ **Bug real encontrado y corregido: errores reales se perdían en un mensaje genérico de Vercel.** En `api/gestion/pacientes.js`, el despachador de acciones hacía `return crearPaciente(req, res)` (sin `await`) — cuando la función interna fallaba, el rechazo de la promesa se escapaba del `try/catch` del handler, y en vez de nuestro JSON de error (`{"error": "Error inesperado."}`) el cliente recibía la página genérica de Vercel ("FUNCTION_INVOCATION_FAILED"), sin ningún detalle. Se agregó `await` a los 9 despachos de acciones. Esto no arregla el error de fondo (ver el punto pendiente abajo) pero permitió ver el error real en los logs de Vercel en vez de estar a ciegas.
- ⚠️→✅ **Bloqueante resuelto: el selector de carpeta (Picker) no era suficiente, había que ampliar el scope a "drive" completo.** Primer intento: `drive.file` + selector de carpeta (Google Picker) para acotar el acceso a la carpeta "Pacientes" — el usuario conectó la carpeta con éxito (confirmado por él mismo, mensaje "✅ Carpeta conectada correctamente"), pero **el problema no era el Picker en sí, sino un límite real de Google**: elegir una carpeta por Picker con scope `drive.file` da acceso a archivos NUEVOS que la app crea ahí, pero NO a los archivos que ya existían adentro — ni a los 40+ pacientes ya cargados, ni a la plantilla `FICHA NO MODIFICAR` que usa "Crear ficha". Confirmado con un diagnóstico temporal que leyó el scope real del token contra `oauth2.googleapis.com/tokeninfo` y con los logs de Vercel (`GaxiosError: File not found` al intentar copiar la plantilla o leer una ficha real). **Solución con el OK del usuario**: cambiar el scope de `drive.file` a `drive` completo (scope sensible, sigue sin pedir verificación de Google mientras el uso quede bajo el límite de 100 usuarios — confirmado en la consola de Google Cloud, "1 usuario del máximo de 100"). Se sacó el paso del selector de carpeta de `gestion/conectar-drive.html` (ya no hace falta, un solo "Conectar con Google" alcanza) y el odontólogo reconectó una vez más con el scope nuevo. **Confirmado funcionando con datos reales**: `listar` devuelve 206 pacientes reales (antes `[]`), `ficha` de un paciente real (Agostina Schulman) devuelve todos sus datos correctamente.
- ✅ **Pruebas de punta a punta completadas, todas contra producción real** (no simuladas):
  - **Crear paciente CON autocompletado**: "Santiago Bertti" (nombre real con un solo turno asociado) → apareció el popup "¿El número de teléfono de Santiago es 3442622964?" → "Sí" completó el campo → ficha creada, navegación automática a la vista de la ficha.
  - **Crear paciente SIN autocompletado**: "Zzztest SistemaPrueba" (nombre ficticio, sin turnos) → no apareció ningún popup → ficha creada igual.
  - **Movimiento "Tratamiento"**: "Limpieza dental", Debe $15.000 → Estado financiero actualizado correctamente (Total $15.000, Saldo $15.000, badge "🔴 DEBE $15.000").
  - **Movimiento "Pago" con "Forma de pago"**: "Pago parcial", Haber $5.000, Forma de pago "Efectivo" (campo de texto libre, tal como se pidió) → Saldo recalculado a $10.000, la columna "Forma de pago" solo se completa en la fila de Pago (vacía en la de Tratamiento).
  - **Sincronización con el Sheet real**: se abrió el archivo real "Santiago Bertti - Ficha" en Drive y se confirmó que el teléfono, los dos movimientos y el valor "Efectivo" en la columna H (antes oculta, ahora visible) coinciden exactamente con lo cargado desde `/pacientes`.
  - **Detección de edición directa en el Sheet**: se editó el campo Domicilio directamente en la hoja real ("Editado directo en Sheet 123") y, al reabrir la ficha en `/pacientes`, el cambio apareció reflejado correctamente.
  - Los dos pacientes de prueba ("Santiago Bertti" y "Zzztest SistemaPrueba") se borraron de la carpeta real de Drive al terminar, para no dejar datos ficticios mezclados con los 206 pacientes reales.

## Módulo de Fichas de pacientes — dos ajustes de UX (2026-08-11)

Dos pedidos sobre el módulo recién entregado en Fase 1: la sección "Estado de prestaciones a obra social" que faltaba, y simplificar "Agregar movimiento" para que el tipo (Tratamiento/Pago/Tratamiento+Pago) se infiera solo en vez de elegirse a mano.

- ✅ **Sección "Estado de prestación obra social"**, dentro de la misma ficha (botón nuevo al lado del nombre del paciente, scrollea a una sección más abajo — no navega a otra URL ni abre una ficha nueva, confirmado que la URL queda igual tras el click). Layout real de la planilla (`⭐FICHA NO MODIFICAR`) inspeccionado a mano: título en J12:M13, Obra social/Nº de afiliado/Plan en L14/L15/L16 son **fórmulas** (`=C11`/`=C12`/`=C13`) que espejan los mismos campos de la sección principal — nunca se escriben directo, se sigue usando `CAMPO_CELDA` (C11/C12/C13) como siempre y el espejo se actualiza solo. Tabla de prestaciones (Fecha/Tratamiento/Código/Autorizado) en J17:M2000, mismo arranque de fila (18) que la tabla de movimientos pero columnas independientes.
- ✅ **Los tres campos son compartidos, no una copia**: Obra social/Nº de afiliado/Plan tienen el mismo `data-campo` en las dos secciones (principal y obra social), así que el autoguardado ya genérico los cubre a ambos automáticamente; se agregó además un paso explícito de "espejar el valor recién guardado en la instancia hermana" para que el cambio se vea al instante en la otra sección sin recargar la página. **Confirmado en los dos sentidos** contra un paciente de prueba temporal: editar Nº de afiliado desde la sección de obra social lo actualiza en la ficha principal, y editar Obra social desde la ficha principal lo actualiza en la sección de obra social — sin recarga.
- ✅ **CRUD de prestaciones** (`prestacion-agregar/editar/eliminar`, mismo patrón de respaldo-ante-fallo que movimientos) probado de punta a punta: se cargó una prestación (fecha, tratamiento, código, tildada "Autorizado"), se confirmó que renderiza con ✅ en la app, y se verificó en la planilla real que cayó en la fila correcta (J18:M18) con un **checkbox real tildado en la columna M** (no el texto "TRUE") — y que L14/L15 (fórmulas espejo) efectivamente mostraban los valores actualizados de Obra social/Nº de afiliado, confirmando que el vínculo de fórmula preexistente de la plantilla seguía intacto.
- ✅ **"Agregar movimiento" simplificado**: se sacó el `<select>` de tipo y los campos "Debe"/"Haber" pasaron a un solo formulario con un hint en vivo ("Se va a guardar como: Tratamiento" / "Pago" / "Tratamiento + Pago") que se recalcula con cada tecleo según qué campos tengan un monto. "Forma de pago" se muestra solo si hay un monto en Haber. Cero cambios de backend: se confirmó primero, leyendo el código existente, que el concepto de "tipo" nunca se persistía en la Sheet ni se mandaba en el payload (`movimiento-agregar/editar` solo reciben fecha/tratamiento/debe/haber/formaPago) — es un cambio de UX puro, la lógica financiera (saldo automático vía fórmula, anular en vez de borrar, recálculo de saldos posteriores) no se tocó.
- ✅ **"Carga histórica"** como botón aparte (no un toggle dentro del mismo form): mismo formulario, título cambia a "Carga histórica" y "Forma de pago" queda siempre visible (para pagos viejos donde puede hacer falta aunque el monto de Haber no dispare la lógica automática). Probado cargando un pago con fecha vieja (01/03/2025): la fecha se guardó tal cual (no se fuerza a hoy) y quedó bien ubicada en la tabla con su saldo acumulado correcto.
- ✅ **Las tres combinaciones de auto-detección probadas contra un paciente de prueba real**: solo Debe → "Tratamiento" (Forma de pago oculto), solo Haber → "Pago" (Forma de pago visible y obligatorio en la práctica), ambos → "Tratamiento + Pago". Saldo recalculado correctamente en cada paso ($1.000 → $500 → $1.700 tras las tres cargas). Regresión de "Anular" confirmada: al anular un movimiento, la fila queda tachada con `[ANULADO]` en el tratamiento, Debe/Haber en 0, y el saldo de las filas siguientes se recalculó solo (de $1.700 a $1.400 tras anular el tercer movimiento).
- ✅ **Bug real encontrado y corregido: el formulario "Editar movimiento" no precargaba Debe/Haber.** La API devuelve esos dos campos ya formateados por Sheets como moneda (ej. `"$1.000,00"`), y asignar ese string directo a un `<input type="number">` falla en silencio (el navegador lo rechaza y deja el campo vacío) — Fecha, Tratamiento y Forma de pago sí precargaban bien porque son campos de texto. Se agregó `montoSheetANumero()` (`pacientes/index.html`) que limpia el formato antes de asignarlo. Encontrado probando el formulario de edición contra un movimiento real ya guardado; confirmado corregido tras el fix, contra el mismo movimiento.
- ✅ **Limpieza**: paciente de prueba temporal "Zzztest ObraSocialPrueba" (creado para no dejar marcas `[ANULADO]` permanentes en el historial de un paciente real) borrado de Drive al terminar.

**Nota de proceso**: durante esta vuelta se encontró en el working tree del repo un conjunto de cambios sin commitear en `api/gestion/pacientes.js` y `lib/pacientesSheet.js` — detección de pacientes duplicados por DNI, fusión manual de fichas duplicadas, y normalización a mayúsculas de varios campos — que **no formaban parte de ese pedido ni de esa sesión** y todavía no tenían ningún frontend que los usara (ninguna llamada a `accion: 'fusionar'` ni chequeo de duplicado en `pacientes/index.html`). Se dejaron intactos sin commitear en su momento. Esos cambios eran el arranque de la Fase 2 — ver la sección siguiente, donde se terminó el frontend, se corrigió un bug real encontrado en el camino, y se commitearon.

## Módulo de Fichas de pacientes (Fase 2) — duplicados por DNI, mayúsculas, pulido UX (2026-08-11, tercera vuelta)

Pedido explícito: los tres puntos que habían quedado fuera de la Fase 1 (duplicados/DNI) más dos pedidos nuevos (mayúsculas automáticas, pulido de UX al nivel de `/gestion`).

1. ✅ **Detección de duplicados por DNI — único criterio, nunca nombre/apellido solos.**
   - **Al crear un paciente**: chequeo instantáneo contra la lista ya cargada en memoria (sin ida y vuelta al servidor) apenas se completa el DNI (blur) o al confirmar — si ya existe, se bloquea la creación y se muestra "Ya existe una ficha con este DNI: [nombre]" con botón "Abrir ficha" que lleva directo a la ficha existente. Además, `crearPaciente` (backend) hace el mismo chequeo como red de seguridad server-side, por si la lista del cliente estuviera desactualizada.
   - **Escaneo continuo**: la pantalla principal de `/pacientes` recalcula duplicados cada vez que se lista (usa el mismo `listar` de siempre, que ya devuelve el DNI de cada ficha — no hizo falta ningún endpoint nuevo) y además cada 2 minutos mientras hay sesión activa, salteando el ciclo si hay un panel abierto o el buscador tiene foco (mismo criterio que el auto-refresh de `/gestion`). Si hay algún DNI repetido, aparece un cartel "❗ Hay fichas duplicadas" con botón "Revisar".
   - **Fusión siempre manual**: "Revisar" abre una vista con los grupos de fichas por DNI repetido → elegir 2 (o las 2 únicas, ya preseleccionadas) → pantalla de comparación campo a campo. Los campos donde coinciden o donde solo una tiene dato se completan solos; un dato contradictorio entre las dos **arranca vacío y bloquea "Confirmar fusión"** hasta resolverlo a mano (click sobre uno de los dos valores para copiarlo, o escribir uno nuevo). Elegir cuál ficha se mantiene es un paso aparte, también obligatorio. Recién con todo resuelto aparece la confirmación explícita (mismo componente `confirmDialog` de siempre) antes de fusionar — el sistema nunca decide ni fusiona solo.
   - **Backend, acción nueva `fusionar`**: migra todos los movimientos y prestaciones de la ficha que se va a la que queda (append al final de lo que ya tenía), escribe los campos ya resueltos, y manda la ficha vieja a la **papelera de Drive** (`trashed: true`, nunca borrado permanente — se puede recuperar a mano si hace falta).
2. ✅ **Mayúsculas automáticas**: Nombre, Apellido, Localidad, Domicilio, Obra social, Plan y Tratamiento (tanto "Plan de tratamiento" de la ficha como la descripción de cada movimiento) se transforman a mayúsculas en vivo mientras se tipea, sin importar cómo las escriban, con el mismo criterio reforzado del lado del servidor (`CAMPOS_MAYUSCULAS`/`aMayusculas()` en `lib/pacientesSheet.js`) para que quede cubierto también el reintento de respaldos de emergencia. DNI, Nº de afiliado, teléfono y fecha de nacimiento quedan sin tocar (campos numéricos/de identificación, no texto legible en mayúscula). Por criterio propio se sumaron Forma de pago y Código de prestación (texto corto de identificación, mismo espíritu que lo pedido).
3. ✅ **Pulido de UX al nivel de `/gestion`**: las 4 vistas del módulo (lista, ficha, duplicados, fusión) y los paneles que se despliegan (nuevo paciente, movimiento, prestación) ahora usan la misma transición de entrada que el resto del sitio (`mostrarCardAnimada`/`.card-visible`, fade + `translateY`, 200ms `ease`) en vez de aparecer/desaparecer instantáneo con `.hidden`. Resaltado breve (mismo `.row-highlight` que `/gestion`) en la fila recién creada o recién fusionada al volver a la lista. Toast nuevo "✅ Fichas fusionadas" para la acción de fusión.
4. ✅ **Bug real encontrado y corregido durante las pruebas**: elegir "Mantener esta ficha" en la pantalla de fusión volvía a construir toda la tabla de comparación desde cero (`renderFusion()` recalculaba todo), así que cualquier conflicto que la secretaria ya hubiera resuelto a mano se perdía en silencio apenas elegía cuál ficha mantener. Se separó en dos funciones: `renderFusion()` arma la tabla una sola vez (al cargar las dos fichas), `actualizarBotonesMantener()` solo cambia el estado visual de los dos botones "Mantener esta" sin tocar la tabla. Confirmado el fix: resolver los 4 campos en conflicto, elegir "Mantener esta" y verificar que los 4 valores seguían intactos antes de confirmar.
5. **Probado de punta a punta con datos simulados** (servidor estático local + `fetch` mockeado con datos de prueba en memoria — sin credenciales de Google en esta máquina, mismo motivo de siempre): DNI duplicado al crear (cartel + "Abrir ficha" navegando a la ficha correcta), mayúsculas transformándose en vivo al tipear, cartel de duplicados apareciendo con datos del mock, y el flujo completo revisión → comparación → resolución de 4 campos en conflicto (Nombre/Apellido/DNI/Localidad) → elegir cuál mantener → confirmación → fusión, con el payload final enviado al backend verificado campo por campo (incluidos los campos sin conflicto que se autocompletaron con el único valor disponible) y la navegación a la ficha ya fusionada confirmando los datos combinados.
6. ⚠️ **No probado contra Drive/Sheets real ni contra los ~206 pacientes reales ya cargados** — no hay `GESTION_KEY` ni credenciales de Google en esta máquina. En particular, **no se pudo revisar si hay duplicados reales entre los pacientes ya cargados en producción**, que era un pedido explícito de esta vuelta. Con el cartel ya desplegado, la forma más simple de confirmarlo es entrar a `/pacientes` en producción (con la `GESTION_KEY` real) y mirar si aparece el cartel "❗ Hay fichas duplicadas" en la pantalla principal — si aparece, "Revisar" ya lleva al flujo de fusión completo tal como se probó acá.

**Nota (backfill)**: entre esta vuelta y la siguiente se commiteó por separado "Obra social: vista separada + checkbox rápido de Autorizado" — la sección "Estado de prestación obra social" pasó de panel apilado debajo de la ficha a su propia vista (`vista-obra-social`, mismo `cambiarVista()`), el checkbox "Autorizado" de la tabla ahora se tilda/destilda con un clic directo (reusa `prestacion-editar`, sin abrir el form completo), y se corrigió un filtro de filas vacías/con `"FALSE"` de checkbox mal aplicado en fichas viejas. No quedó documentado en su momento; se deja registrado acá para que el historial sea coherente.

## Módulo de Fichas de pacientes (Fase 3) — acceso rápido "Pacientes de hoy" + simplificación (2026-08-11, cuarta vuelta)

Pedido explícito del odontólogo tras usar el módulo unos días: lo sintió "más difícil" que la planilla vieja de Google Sheets (donde tenía todas las fichas de los pacientes del día pre-abiertas en pestañas). Tres puntos, el primero marcado como el más importante.

1. ✅ **Carrusel "Pacientes de hoy"** — el cambio más importante. Arriba de todo en la pantalla principal de `/pacientes` (la lista/búsqueda de siempre queda exactamente igual debajo, sin mezclarse con la agenda). Cruza los turnos de hoy (`api/gestion/turnos-dia.js`, reusa la misma `GESTION_KEY` que ya usa todo `/pacientes` — no hizo falta ninguna ruta ni endpoint nuevo) contra las fichas ya cargadas, con **coincidencia exacta** de nombre+apellido normalizado (sin acentos/mayúsculas, espacios colapsados) — mismo criterio best-effort que ya usaba el autocompletado de teléfono al crear paciente. Un turno de hoy sin ficha con ese nombre exacto **no aparece** (tal como se pidió) — no se crea nada solo, y no queda ningún vínculo permanente entre el turno y la ficha, es puramente una ayuda de acceso rápido que se recalcula en cada carga de la lista y en cada ciclo del escaneo periódico de 2 minutos que ya existía (no se sumó ningún timer nuevo). Bloqueos del día se excluyen. Cada chip (avatar circular con iniciales, nombre, hora en horario Argentina explícito) abre la ficha correspondiente con un solo toque. La sección entera queda oculta si no hay ninguna coincidencia ese día (mismo criterio que el cartel de duplicados).
2. ✅ **Botón "Volver" prominente**: los 4 links de vuelta (ficha → lista, duplicados → lista, fusión → duplicados, obra social → ficha), antes texto chico subrayado (`.volver-ficha`) fácil de pasar por alto, ahora son un botón pill claro y siempre visible arriba de cada vista (`.btn-volver`), con el mismo lenguaje visual que `.copy-link-btn` de `/gestion` (fondo, borde, hover dorado) — se cambiaron los 4, no solo el de la ficha, para no dejar el resto del módulo inconsistente.
3. ✅ **Pasada de simplificación**: se agrandaron los botones principales de las acciones más comunes — "+ Nuevo paciente", "Crear ficha"/"Guardar"/"Cancelar" de los paneles, "+ Agregar movimiento"/"+ Agregar prestación" — y se agrandó el tap target de los íconos de editar/anular movimiento (26px→32px). Alcance acotado a propósito: no se rediseñó el flujo de crear paciente ni el de cargar un movimiento porque ya eran de un solo paso (abrir el formulario ya visible → completar → guardar) — la fricción real que describió el odontólogo (navegar, encontrar la ficha correcta) queda resuelta principalmente por los puntos 1 y 2.
4. **Probado de punta a punta con datos simulados** (servidor estático local + `fetch`/`turnos-dia` mockeados, sin credenciales de Google en esta máquina): un turno de hoy con ficha coincidente aparece en el carrusel con la hora correcta ya convertida a horario Argentina; un turno con un nombre que no matchea ninguna ficha no aparece; un bloqueo del día no aparece; tocar un chip abre la ficha directo (sin pasar por la lista ni el buscador); "Volver a la lista" (ya con el estilo de botón nuevo) regresa a la pantalla principal con el carrusel recalculado. **No probado contra producción real** — no hay `GESTION_KEY` en esta máquina, mismo motivo que las vueltas anteriores.

## Estado de prestación obra social: 3 ajustes + bug crítico de arranque (2026-08-12)

Pedido explícito sobre lo entregado en la vuelta anterior (ver nota de backfill más arriba): separar la sección de obra social en una vista propia (no apilada debajo de la ficha), sacar las filas vacías/falsas de la tabla de prestaciones, y que el checkbox "Autorizado" se pueda tildar con un clic sin entrar a editar. Los tres puntos ya estaban implementados (ver nota de backfill), así que esta vuelta fue sobre todo de **prueba de punta a punta contra un paciente real con el bug efectivamente reproducido**, y encontró un bug bloqueante nuevo en el camino.

1. ✅ **Filas falsas — causa raíz confirmada, no solo el síntoma parcheado.** Se escanearon los 206 pacientes reales vía la API (`modo: ficha` para cada uno) buscando cuáles tenían `prestacionesObraSocial` no vacía: 30 de 206 mostraban **1983 filas cada uno** con `tratamiento: "FALSE"` — un checkbox de Sheets (probablemente de una versión vieja de la plantilla, antes de corregirse) aplicado por error sobre la columna Tratamiento en vez de (o adicionalmente a) la de Autorizado, que en cualquier fila sin completar devuelve el string `"FALSE"` en vez de vacío. El fix (`celdaTextoLimpia()` en `api/gestion/pacientes.js`) descarta cualquier valor `true`/`false`/`'TRUE'`/`'FALSE'` de Tratamiento y Código antes de decidir si la fila tiene datos reales — soluciona el síntoma para cualquier fichero afectado, sin necesidad de tocar las 30 planillas una por una.
2. ✅ **Confirmado contra un paciente real con el bug reproducido** ("Alvaro Drozdov", uno de los 30 encontrados en el escaneo): antes del fix mostraba las 1983 filas falsas; después, "Sin prestaciones cargadas todavía." — limpio. Se agregó una prestación de prueba real, se confirmó que el checkbox de Autorizado cambia con un solo clic (sin abrir el form de edición, toast "✅ Actualizado" o pendiente de sincronizar según corresponda) y que la vista se abre separada de la ficha (título propio "🏥 Estado de prestación obra social", subtítulo con nombre+DNI del paciente, botón "← Volver a la ficha", la ficha principal no se ve mientras está abierta). La prestación de prueba se eliminó al terminar, sin dejar rastro.
3. ✅→**Bug real, bloqueante, encontrado y corregido: la app no arrancaba para una sesión ya logueada.** Detectado por accidente al recargar la página para probar el fix de arriba — quedaba trabada en "Cargando pacientes…" para siempre. Causa confirmada en la consola real del navegador: `arrancar()` se dispara de forma síncrona cuando ya hay `gestionKey` en `sessionStorage` (reingreso), pero en ese momento el script todavía no llegó a declarar `patientListEl`/`duplicadosTimer` (usadas por `cargarLista()`/`iniciarEscaneoDuplicados()`, ambas llamadas desde `arrancar()`) — temporal dead zone, `ReferenceError`. Este bug **no tiene nada que ver con el pedido de esta vuelta**; ya estaba en el código de una vuelta anterior (Fase 2/3), simplemente nadie lo había disparado todavía porque hace falta tener `gestionKey` ya guardada al cargar la página (primera vez que alguien recarga en vez de loguearse de cero). Fix: `if (gestionKey) queueMicrotask(arrancar);` en vez de la llamada síncrona — difiere el arranque hasta que el script termina de declarar todo, sin parpadeo visible. **Confirmado en producción**: recarga con sesión ya logueada carga la lista de pacientes normalmente, sin errores nuevos en consola.
4. **Nota de proceso**: el archivo `pacientes/index.html` tuvo edición concurrente activa de otra sesión/ventana durante esta vuelta (fue la que agregó "Pacientes de hoy" y los botones `.btn-volver`, documentado arriba en Fase 3) — el fix del punto 3 se aisló con un patch de un solo hunk (`git apply --cached`) para commitear solo ese cambio puntual sin interferir con el trabajo en curso de la otra sesión, que siguió su curso y se commiteó por su cuenta poco después.

## Módulo de Fichas de pacientes (Fase 4) — layout desktop, sidebar, autocompletado cruzado (2026-08-12, segunda vuelta)

Pedido explícito, con una aclaración inicial pedida por el usuario ("si no estás seguro cómo hacer algo, preguntame antes") — no hizo falta ninguna pregunta bloqueante, las decisiones de diseño (breakpoint, ancho de columnas, alcance del autocompletado a "hoy" para la dirección ficha→turno) se tomaron con criterio propio y quedan documentadas acá para que se puedan revisar.

1. ✅ **Aprovechar el espacio horizontal en desktop.** `#app` pasa de `max-width:720px` fijo (mismo ancho en cualquier pantalla, con márgenes enormes en una PC) a un layout de dos columnas en `≥1000px` (mismo breakpoint que `/gestion`): `.sidebar` (260px) + `.main-content` (hasta 900px). **Mobile no se tocó**: `.sidebar{display:none}` por defecto, `.main-content` conserva el `max-width:720px` de siempre por debajo del breakpoint. Dentro del detalle de una ficha, "Datos del paciente" pasa a una columna izquierda fija (340px) y "Estado financiero" + "Movimientos" (+ el formulario de nuevo movimiento) a una columna derecha que aprovecha el resto del ancho, en vez de las tres secciones apiladas siempre a lo largo sin importar el ancho de pantalla.
2. ✅ **Sidebar con resumen, mismo estilo y datos que `/gestion`.** "Hola, Franco" + fecha de hoy (`Intl.DateTimeFormat('es-AR', {weekday:'long', day:'numeric', month:'long'})` con `timeZone` explícito, nunca getters locales — regla del proyecto), "N turnos hoy" y "N huecos libres hoy". Reusa el mismo fetch a `turnos-dia.js` que ya hacía "Pacientes de hoy" (un solo request cumple las dos funciones, `cargarDatosDeHoy()` unificó lo que antes era `cargarPacientesDeHoy()`) más un fetch nuevo a `api/disponibilidad?modo=dia` (endpoint público existente, sin `GESTION_KEY`) para los huecos libres — **no se sumó ningún endpoint nuevo**, seguimos en 12/12 funciones serverless.
3. ✅ **Tareas pendientes en el sidebar, únicamente fichas duplicadas por DNI** — a propósito sin ningún otro tipo de tarea, tal como se pidió. Mismo estilo visual `.task-item.urgent` que `/gestion`, reusando los mismos grupos que ya calculaba `calcularGruposDuplicados()` para el cartel de la pantalla principal (ese cartel se dejó intacto, no se sacó — queda una redundancia visual leve en desktop entre el cartel y la tarea del sidebar, aceptada a propósito para no tener que ocultar el cartel solo en un breakpoint y complicar el código sin necesidad real). "Revisar" navega directo a la vista de duplicados. Sección oculta por completo si no hay ninguna ficha duplicada pendiente.
4. ✅ **Autocompletado cruzado de teléfono, automático y sin popup, solo cuando el campo está vacío** (nunca sobrescribe un dato ya cargado, tal como se pidió):
   - **Turno → ficha**: al abrir una ficha sin teléfono, se usa el mismo `telefono-turnos` que ya existía (búsqueda amplia por nombre+apellido exacto, no limitada a "hoy" — decisión de alcance: acá no hacía falta acotar porque el endpoint ya era liviano y solo se llama una vez por ficha abierta) — si hay una única coincidencia, se completa el campo y se guarda solo (`actualizar-campo`), sin ningún diálogo de confirmación. A propósito distinto del autocompletado que ya existía al *crear* un paciente, que sí pregunta con un popup — acá no hace falta porque la ficha ya existe y solo se está completando un dato faltante, tal como se pidió explícitamente.
   - **Ficha → turno**: **alcance acotado a propósito a los turnos de HOY** (no a todo el historial de turnos) — reutiliza el mismo cruce que ya hacía "Pacientes de hoy", así no hace falta ninguna búsqueda nueva y cara por nombre en todo el calendario. Para cada turno de hoy sin teléfono cargado cuya ficha coincidente sí tiene uno, se escribe en la descripción del evento de Calendar. Acción nueva `completar-telefono-turno` en `api/gestion/pacientes.js` (sin sumar ningún archivo), con un re-chequeo defensivo de que el evento siga sin teléfono justo antes de escribir (por si pasó tiempo entre que se armó la lista de "hoy" y esta llamada). El teléfono se escribe tal cual está en la ficha (texto libre, no pasa por `telefonoParaWhatsApp()` como si viniera del selector de país, porque no viene de ahí).
5. ✅ **Botón "Volver a gestión" condicional y más prominente.** Solo aparece si se entró a `/pacientes` desde `/gestion` (`document.referrer` incluye `/gestion`) en esa sesión de pestaña — persistido en `sessionStorage` (`vinoDesdeGestion`) para sobrevivir un F5 sin perder el estado, se borra solo al cerrar la pestaña. Si se entra directo por URL o bookmark, no se muestra nada. Cuando se muestra, usa el mismo estilo de botón pill claro (`.btn-volver`) que ya se usó para los 4 links de "Volver" de la vuelta anterior, en vez del link chico subrayado de antes — mismo criterio pedido explícitamente.
6. **Probado de punta a punta con datos simulados en viewport de escritorio ancho** (servidor estático local + `fetch` mockeado con datos de prueba en memoria, sin credenciales de Google en esta máquina — mismo motivo de siempre): sidebar con datos correctos (2 turnos hoy, 5 huecos libres, tarea de DNI duplicado con "Revisar" funcionando), layout de 2 columnas visible tanto en la pantalla principal (sidebar + contenido lado a lado) como dentro de la ficha (Datos del paciente | Estado financiero+Movimientos lado a lado, confirmado con captura de pantalla), autocompletado ficha→turno confirmado con la llamada real a `completar-telefono-turno` (teléfono correcto, turno mockeado quedó con el dato actualizado), autocompletado turno→ficha confirmado (el campo Teléfono se completó solo y se guardó al abrir la ficha, sin ningún popup), y el botón "Volver a gestión" apareciendo y persistiendo correctamente a través de un reload simulando `sessionStorage`. **No probado contra producción real** — no hay `GESTION_KEY` ni credenciales de Google en esta máquina.

## Pendiente: verificación floja (no bloqueante, pero sin confirmar)

 **Impresión del ticket en blanco y negro estricto (2026-08-07, decimoctava vuelta): filtro CSS revisado en el código, no probado contra la impresora física.** El filtro `grayscale(1) contrast(1000%)` sobre el logo es la técnica estándar para forzar negro/blanco puro sobre una imagen con antialiasing, pero solo una impresión real confirma si el resultado se ve nítido a 58mm con la HPRT TP585L (mismo motivo que el punto de abajo, ya pendiente de la vuelta anterior — ahora hay que revisar además que el logo salga sin ningún gris visible). **Antes de confiar 100%: imprimir un ticket de prueba real y mirar especialmente el logo** (no solo el texto/QR, que ya se sabía que salían bien).
- **Ticket térmico 58mm (2026-08-07, decimoséptima vuelta): probado con datos simulados, no contra la impresora física HPRT TP585L.** El CSS `@page { size: 58mm auto; margin: 2mm }` sigue el patrón estándar documentado para impresoras térmicas de este ancho, y `window.print()` se confirmó disparando el diálogo nativo del navegador sin errores — pero no hay forma de interactuar con el diálogo nativo de impresión del SO ni con una impresora física desde este entorno. **Antes de confiar 100%: imprimir un ticket de prueba real desde la computadora del consultorio** (con la HPRT TP585L conectada) y confirmar que sale a 58mm, con el logo/QR nítidos y todo el texto legible — si el ancho sale mal, puede hacer falta ajustar el ancho de `#ticket-print` en `@media print` (hoy 48mm) según lo que la impresora realmente imprima. También conviene escanear el QR de un ticket real con el celular para confirmar que la cámara lo lee bien a ese tamaño de impresión.
- **Chatbot público con Gemini y function-calling (2026-08-06, decimosexta vuelta): probado con un mock de `/api/chat`, no contra la API real de Gemini ni Google Calendar.** Es lo más importante para confirmar en producción antes de darlo por terminado al 100%: que Gemini efectivamente decida llamar a `consultar_dias_disponibles`/`consultar_horarios`/`crear_turno` en el momento correcto de la conversación (el formato del request está verificado contra la documentación oficial de function-calling de Gemini, pero el comportamiento real del modelo — cuándo elige llamar a cada herramienta — solo se confirma probándolo en vivo), que la reserva conversacional de punta a punta efectivamente cree el turno en el Calendar real, y que el rechazo de teléfonos sin código de país funcione como se espera con respuestas reales del modelo (no simuladas). Probar especialmente: una reserva completa de paciente nuevo (cartel de bienvenida + confeti) y una de paciente recurrente, desde `index.html` y desde `/turnos`.
- **Asistente de ayuda con IA en `/gestion`: probado en producción con la `GESTION_KEY` real (2026-08-07, decimonovena vuelta), pero sin una respuesta real de Gemini todavía.** Se abrió el panel desplegado y se mandó una pregunta real ("como bloqueo un dia completo?") — el endpoint respondió 200 pero con cuota de Gemini agotada (429 "quota exceeded" en los logs de Vercel, de tanto testeo en el día), así que se activó el fallback honesto en vez de una respuesta real del modelo. Se confirmó el fallback, el ícono 🤖 del encabezado, y (con una respuesta simulada) el formato de texto plano/markdown y la estabilidad del layout. **Sigue faltando: una respuesta REAL de Gemini con la cuota ya recuperada**, para confirmar que el tono/formato coincide con lo pedido en el system prompt sin intervención.
- **"Agregar a Apple Calendar" en `/turnos` (2026-08-06, novena vuelta) — sin verificar en iPhone real.** El link se arma sin atributo `download` a propósito (navegación directa a un blob `Content-Type: text/calendar`), que según el comportamiento documentado de iOS Safari debería abrir la pantalla nativa "Agregar evento" en vez de descargar el archivo a Files — pero no se pudo confirmar en esta máquina (sin Xcode completo para el Simulador, sin iPhone físico disponible). **Antes de confiar en que funciona: probarlo desde un iPhone real.** Si sigue yendo a Archivos (o similar) en vez de abrir Calendar directamente, sacar el botón "Agregar a Apple Calendar" y dejar únicamente "Agregar a Google Calendar" (`btn-agregar-google` en `turnos/index.html`, ya implementado y sin esta incertidumbre — abre `calendar.google.com/calendar/render` directo, sin archivo de por medio).
- El flujo de "Editar tel." guardando **vacío** (para borrar un número) se implementó y se revisó el código, pero no se probó en vivo end-to-end (sí se probó extensamente el camino de guardar un número válido). Antes de confiar en que funciona 100%, probarlo una vez desde la interfaz.
- **Selector de país (2026-08-06): probado a fondo del lado del cliente y con datos simulados** (servidor estático local, sin las credenciales de la cuenta de servicio de Google disponibles en esta máquina) — el selector, la búsqueda de país, el cálculo del E.164, el agregado del "9" para Argentina y el payload final que se manda a cada ruta se verificaron uno por uno. **No se probó todavía contra el Calendar real en producción** (crear un turno de punta a punta desde `/turnos` o `/gestion` desplegado, ver el evento real en el calendario, click real al WhatsApp resultante desde el teléfono). Conviene hacer una pasada así después de desplegar, antes de darlo por 100% confirmado en producción.
- **Lista de tareas del sidebar (2026-08-06, con las correcciones de la misma tarde): probada con datos simulados** (mismo motivo — sin credenciales de Google en esta máquina), incluyendo el estado vacío (sección oculta), la tarea "Agregar teléfono" navegando a otro día y abriendo el editor inline, y la tarea "Mover N turnos" de un día bloqueado navegando ahí (sin abrir ningún editor, solo navega). **No probada contra el Calendar real** ni con volumen real de turnos (para confirmar que el límite de 6 tareas visibles + "+N más" se ve bien con una agenda cargada de verdad).
- **Badge "⚠ Tel. inválido" (2026-08-06, corregido): probado con un teléfono legado válido (no se marca) y uno con formato inválido (sí se marca)** con datos simulados. No probado contra el Calendar real.
- **Ítems 5, 6 y 8 (2026-08-06, cuarta vuelta): probados con datos simulados**, sin credenciales de Google en esta máquina — se verificaron uno por uno: generación del `.ics` (fechas UTC correctas), reprogramar y cancelar el turno recién creado desde `/turnos` (mismo `eventId` viaja correctamente en ambos casos), buscador con auto-retorno, "Nuevo turno" con la casilla "Paciente nuevo" (el badge dorado aparece después), edición de teléfono y cancelación de turno en `/gestion` sin recargar toda la agenda, diálogo de confirmación propio, toasts. **No probado contra el Calendar real en producción.** Antes de confiar 100%: reservar un turno real desde `/turnos` desplegado, probar "Cambiar día y horario" y "Cancelar turno" ahí, y confirmar en `/gestion` que un turno creado con "Paciente nuevo" tildado muestra bien el badge.

## Pendiente: ítems 4 y 7 del pedido del 2026-08-06 (sin empezar)

Quedan estos dos del pedido original de 8 puntos — el resto ya está hecho, ver arriba.

### 4. Feriados argentinos + tarea "¿Se atiende este día?"
Detección automática de feriados (necesita una fuente de feriados — no hay ninguna cargada todavía, evaluar una API pública gratuita o una lista fija actualizada a mano por año) + una tarea con ciclo de vida con estado (Sí → se borra; No → ofrece bloquear el día). Es la pieza más compleja de las 2 pendientes — necesita persistencia de estado de tareas en algún lado (hoy no hay ninguna base de datos ni storage propio, todo vive en eventos de Calendar; probablemente haya que "codificar" el estado de la tarea como un evento marcador en el calendario, similar a como se marcan los bloqueos con `BLOCK_MARKER`).

**Nota (2026-08-06):** la tarea "Mover turnos de un día bloqueado" que este ítem iba a necesitar **ya está implementada** (independiente de la detección de feriados — se generaliza a cualquier día bloqueado a mano, no solo feriados). Lo que falta acá es específicamente la detección automática de feriados y la tarea "¿Se atiende este día?" con su ciclo de vida Sí/No.

### 7. Apple Liquid Glass (sutil) en las 3 páginas
Efecto visual tipo "Liquid Glass" de Apple (blur/translucidez tinted según el color de cada botón), muy sutil, manteniendo la paleta actual (navy/dorado/crema). Afecta `index.html`, `turnos/index.html` y `gestion/index.html`. **Distinto de lo ya hecho el 2026-08-06 (cuarta vuelta)**: ese pase fue microinteracciones (hover/active/focus, transiciones, toasts) — no toca el look de blur/vidrio esmerilado que pide específicamente este ítem. Bajo riesgo de romper funcionalidad, pero hay que aplicarlo con cuidado para que "se note sutil" y no termine viéndose como un efecto exagerado.

## Nota sobre alcance

El ítem 4 no es "una tarde de trabajo" — implica decisiones de producto y de diseño técnico que vale la pena confirmar con el usuario antes de programar (fuente de feriados, ciclo de vida de la tarea). No asumir la implementación más simple sin preguntar.
